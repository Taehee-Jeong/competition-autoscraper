# 처음 오신 분께 — 크롤링이 뭐고, 이 봇이 뭘 하는지

이 문서는 **프로그래밍을 몰라도** 읽을 수 있게 썼습니다. 이 폴더가 뭐 하는 물건인지,
"크롤링"이라는 말이 정확히 무슨 뜻인지, 그리고 결과물인 엑셀을 어떻게 읽어야 하는지까지
한 번에 잡는 것이 목표입니다.

문서가 세 개 있습니다. 헷갈리지 않게 정리하면 이렇습니다.

| 문서 | 누구를 위한 것 |
|---|---|
| **입문.md** (지금 이 문서) | 처음 보는 사람. 개념부터 |
| `README.md` | 이 봇을 쓸 사람. 설치·실행·소스 목록 |
| `DEVELOPMENT.md` | 코드를 고칠 사람. 함정과 내부 구조 |

---

## 1. 이 봇이 하는 일

한 문장으로: **공모전 사이트 16곳을 대신 돌아다니며 우리 조건에 맞는 대회를 찾아
엑셀 한 장에 계속 모아주는 프로그램**입니다.

원래 사람이 하던 일을 생각해보면 이해가 빠릅니다. 담당자가 위비티에 들어가서, 목록을
훑고, 괜찮아 보이는 걸 클릭해서, 응모대상이 대학생인지 보고, 팀으로 나갈 수 있는지 보고,
마감이 너무 임박하진 않았는지 보고, 엑셀에 한 줄 옮겨 적습니다. 그걸 사이트 열여섯 곳에
대해 매주 반복하는 겁니다.

이 봇은 그 반복을 대신합니다. 사람이 하는 일은 마지막에 엑셀을 열어 확인하는 것만
남습니다.

```
사이트 16곳  →  [ 봇 ]  →  엑셀 한 장  →  사람이 검수
```

---

## 2. 크롤링이 뭔가요

### 웹페이지는 사실 글자 덩어리입니다

브라우저로 공모전 페이지를 열면 표가 예쁘게 그려져 있고, 제목은 굵고, 마감일 옆에는
빨간 D-4가 붙어 있습니다. 그런데 **그건 브라우저가 그려준 그림**이고, 서버가 실제로
보내준 것은 이런 글자 덩어리입니다.

```html
<li> <span class="tit">응모대상</span> 제한없음 </li>
<li class="dday-area"> <span class="tit">접수기간</span> 2026-06-01 ~ 2026-07-30
    <span class="cil-dday">D-4</span> </li>
```

이걸 **HTML**이라고 합니다. `<li>`, `<span>` 같은 꺾쇠 표시는 "여기부터 여기까지가
목록 항목이다", "이건 제목이다" 같은 **표시(태그)** 입니다. 브라우저는 이 표시를 읽고
화면을 그립니다. 위 두 줄이 화면에서는 이렇게 보입니다.

```
응모대상   제한없음
접수기간   2026-06-01 ~ 2026-07-30   D-4
```

### 크롤링 = 사람 대신 프로그램이 그 글자 덩어리를 받아오는 것

프로그램은 브라우저 없이도 서버에 "이 주소의 내용 주세요"라고 요청해서 저 HTML을 그대로
받을 수 있습니다. 받아온 다음 꺾쇠 표시를 길잡이 삼아 **필요한 부분만 골라내는 것**이
크롤링입니다.

- **크롤링(crawling)** — 링크를 타고 돌아다니며 페이지를 모으는 것
- **스크래핑(scraping)** — 모은 페이지에서 원하는 값을 뽑아내는 것

엄밀히는 다르지만 실무에서는 그냥 섞어서 "크롤링"이라고 부릅니다. 이 프로젝트도 둘 다
합니다. 목록 페이지를 돌며 대회 주소를 모으고(크롤링), 각 대회 페이지에서 마감일·응모대상을
뽑습니다(스크래핑).

### 두 가지 방식이 있습니다

| 방식 | 받아오는 것 | 장점 | 단점 |
|---|---|---|---|
| **HTML 긁기** | 사람이 보는 화면의 원본 | 어느 사이트든 가능 | 사이트 디자인이 바뀌면 깨짐 |
| **API 쓰기** | 데이터만 담긴 깔끔한 형식 | 안정적이고 빠름 | 사이트가 열어줘야 가능 |

API는 사이트가 자기 화면을 그리려고 이미 쓰고 있는 데이터 통로입니다. 화면에는 안 보이지만
브라우저가 뒤에서 조용히 받아오고 있는 것이고, 이런 모양입니다.

```json
{"title": "제3회 …공모전", "finish_dt": "2026-08-31", "enter_qualified_nm": "대학생"}
```

보시다시피 꺾쇠 표시를 걷어낼 필요가 없습니다. 값이 이름표와 함께 바로 옵니다.
**그래서 API가 있으면 무조건 API를 씁니다.** 우리 소스 16곳 중 씽굿·링커리어·데이콘·
Devpost·Unstop·MLH·Zindi가 API 방식이고, 위비티·콘테스트코리아·캠퍼스픽 등은 HTML 방식입니다.

> 참고로 예전 문서에는 "씽굿·링커리어·MLH는 자바스크립트로 그려서 수집 불가"라고 적혀
> 있었는데, 실제로 확인해보니 전부 내부 API를 쓰고 있어서 그냥 가져올 수 있었습니다.
> **화면에 목록이 안 보인다고 포기하지 말고 API를 먼저 찾아보라**는 교훈입니다.

---

## 3. 크롤링이 어려운 이유 — 이 프로젝트에서 실제로 겪은 것들

크롤링은 "페이지 받아서 글자 뽑기"라 쉬워 보이지만, 실제로 부딪히는 문제는 다른 데 있습니다.
아래는 전부 이 프로젝트에서 **실제로 일어난** 일입니다.

### (1) 페이지를 통째로 가져오면 메뉴가 딸려 옵니다 — 가장 크게 데였던 것

공모전 페이지에는 공고 내용만 있는 게 아닙니다. 왼쪽에 카테고리 메뉴가 있고, 위에
검색창이 있고, 아래에 회사 정보가 있습니다. 페이지 전체 글자를 그냥 퍼오면 이런 게
전부 섞여 들어옵니다.

```
공모전 · 공모전전략 · 대외활동 · 이벤트참여 ...          ← 왼쪽 메뉴
응시대상자: 전체 / 제한없음 / 일반인 / 대학생 / 청소년    ← 검색 필터
─────────────────────────────────
2026 한국수력원자력 유튜브 짧은영상 공모전                ← 여기부터가 진짜 공고
■ 공모자격: 개인(한국수력원자력 직원 제외)
```

봇은 "이 글에 **대학생**이라는 말이 있으면 대학생이 참가할 수 있는 대회"라고 판단합니다.
그런데 저 검색 필터에 `대학생`이 늘 적혀 있으니, **모든 공고가 "대학생 참가 가능"으로
나왔습니다.** 실제로 확인해보니 다섯 건을 뽑아 전부 `대학생`, `누구나`, `제한없음`이
걸렸습니다. 참가 자격을 한 글자도 언급하지 않은 공고까지 "확인 완료"로 표시된 겁니다.

같은 이유로 왼쪽 메뉴의 `공모전전략`이라는 항목 때문에 `전략`이라는 단어가 모든 페이지에서
발견됐고, 봇이 규칙을 스스로 고치는 기능이 이 `전략`을 "팀 참가 가능 신호"로 학습해버린
사고도 있었습니다.

**교훈: 페이지 전체가 아니라 공고 본문만 잘라내야 합니다.** 지금은 위비티 페이지에서
`상세내용` 영역만 골라 쓰고 있고, 그 결과 본문이 2,600~4,400자에서 **1,000자 안팎**으로
줄었습니다. 줄어든 만큼이 전부 메뉴와 사이드바였다는 뜻입니다.

### (2) 사이트마다 표기가 제각각입니다

같은 "마감일"인데 사이트마다 이렇게 씁니다.

```
2026-07-30                     위비티
2026. 6. 1.(월)~7. 30.(목) 17:00   공고 본문
Jul 21 - Aug 25, 2026           Devpost
2026-08-31 23:59:00.0           씽굿
```

전부 `2026-07-30` 같은 하나의 형식으로 바꿔줘야 "마감까지 2주 남았나?"를 계산할 수
있습니다. 소스마다 이 변환기를 따로 만들어야 합니다.

### (3) 같은 대회가 여러 사이트에 올라옵니다

국내 사이트 다섯 곳이 같은 공고를 각자 올립니다. 문제는 제목이 미묘하게 다르다는 것입니다.

```
2026 MBC 대학가요제        ← 콘테스트코리아
2026 MBC 대학 가요제       ← 위비티              (띄어쓰기 하나)
```

지금은 공백·괄호·이모지를 지운 뒤 비교해서 이런 건 한 줄로 합칩니다. 다만
`…학술대회`와 `…학술대회 공모전`처럼 **단어가 더 붙은 건 일부러 안 합칩니다.**
비슷한 걸 다 합치기 시작하면 `Global Hack Week: Data`와 `Global Hack Week: Agents`
같은 서로 다른 대회까지 하나로 만들어버리기 때문입니다. 실측하면 유사한 18쌍 중 5쌍이
합쳐지고 13쌍은 남습니다. 남는 건 비고란의 소스 표기를 보고 사람이 판단하면 됩니다.

### (4) 사이트가 바뀌면 깨집니다

크롤링 코드는 "그 사이트가 지금 이렇게 생겼다"는 가정 위에 서 있습니다. 사이트가 리뉴얼하면
그 가정이 무너집니다. 그래서 이 프로젝트는 **깨질 때 조용히 틀린 답을 내지 않고 경고를
남기도록** 만들어 뒀습니다. 로그에 이런 줄이 보이면 사이트가 바뀐 것입니다.

```
wevity WARNING 본문 영역(.comm-desc) 없음 — 라벨 정보만으로 판정 ix=107425
```

### (5) 정보가 없는 것과 "아니오"는 다릅니다

공고에 팀 참가 얘기가 아예 없을 때, 봇이 "팀 안 됨"이라고 단정하면 멀쩡한 대회를 버리게
됩니다. 그래서 이 봇은 판정을 **세 가지**로 합니다.

- `통과` — 근거를 찾았고 조건에 맞음
- `탈락` — 안 된다는 근거를 찾음 (버림)
- `확인필요` — 판단할 근거가 없음 (**버리지 않고 남김**)

설계 원칙은 **"놓치느니 남긴다"** 입니다. 애매하면 사람이 보게 넘깁니다.
엑셀에 `확인필요`가 많은 건 고장이 아니라 정상입니다.

---

## 4. 지켜야 할 예의와 규칙

크롤링은 남의 서버에 요청을 보내는 일입니다. 기술적으로 되는 것과 해도 되는 것은 다릅니다.

- **요청 사이에 1초씩 쉽니다.** 쉬지 않고 두드리면 상대 서버에 부담이 되고, 차단당합니다.
  이 값은 빠르게 하고 싶어도 건드리지 마세요.
- **robots.txt를 확인합니다.** 사이트 주소 뒤에 `/robots.txt`를 붙이면 나오는 파일로,
  "여기는 프로그램이 긁어가지 말아 달라"는 사이트 주인의 요청입니다. 법은 아니지만 지킵니다.
  새 소스를 추가할 때 이걸 먼저 확인했습니다.
- **이용약관을 봅니다.** 수집한 데이터는 **교내 공지용으로만** 씁니다. 재배포하거나
  외부 서비스로 만들지 않습니다.
- **로그인이 필요한 곳은 건드리지 않습니다.** 지금 16개 소스 전부 로그인·API 키 없이
  공개된 것만 가져옵니다.

---

## 5. 이 봇이 돌아가는 순서

```
1) 수집        16개 소스에서 공고를 긁어옵니다
                 └ 각 소스는 "제목·마감일·응모대상·본문" 같은 같은 모양으로 맞춰 넘깁니다
       ▼
2) 판정        조건 세 개를 자동으로 봅니다
                 ├ 재학생이 참가할 수 있나?
                 ├ 팀(2인 이상)으로 나갈 수 있나?
                 └ 마감까지 2주 이상 남았나?
                 └ 하나라도 '탈락'이면 버리고, 애매하면 '확인필요'로 남깁니다
       ▼
3) 보강        컴퓨터에 켜둔 작은 AI가 공고를 읽고
                 팀 인원 / 대회 일정 / 참가비 / 학부 분류를 채웁니다 (선택 기능)
       ▼
4) 출력        엑셀에 새 대회만 추가합니다 (이미 있는 건 건너뜀)
```

3번의 AI는 **이 컴퓨터 안에서 도는** 작은 모델입니다(Ollama, qwen2.5:3b).
외부에 데이터를 보내지 않고, API 키도 비용도 들지 않습니다. 꺼져 있으면 그냥 3번을
건너뛰고 규칙만으로 돌아갑니다 — 대신 팀 인원·일정·참가비 칸이 `확인필요`로 남습니다.

> **작은 AI를 쓸 때의 함정 하나.** 처음에는 AI에게 답할 형식을 알려주면서
> `"참가비": "예: '무료' 또는 금액"` 처럼 **보기를 같이 적어줬습니다.** 그랬더니 모델이
> 그 보기를 "이렇게 답하라는 뜻"으로 받아들여서, 참가비가 안 적힌 공고에도 전부 `무료`라고
> 답했습니다. 채워진 229칸 중 200칸이 `무료`였습니다. 보기를 안 준 `대회 일정` 칸만
> 멀쩡했고요. 지금은 보기를 빼서 고쳤습니다.
>
> 작은 모델은 **모른다고 말하기보다 그럴듯한 걸 지어내는 쪽**으로 기웁니다. 그래서
> AI가 "이 대회는 부적합"이라고 해도 **지우지 않고 `신규후보(LLM의심)`으로 표시만** 합니다.
> 판단은 사람이 합니다.

---

## 6. 직접 돌려보기

폴더 이름에 **공백이 있어서** 따옴표가 꼭 필요합니다.

```bash
cd "/home/schoaq/competition-autoscraper 2"
```

가장 안전한 것부터 해보세요. 아래 명령은 **엑셀을 건드리지 않고** 위비티에서 한 건만
받아와 보여줍니다.

```bash
/usr/bin/python3 -c "
from scraper import wevity
ids = wevity.list_contest_ids(pages=1)
d = wevity.fetch_contest_detail(ids[0])
print(d['title']); print('마감:', d['deadline']); print(d['body'][:300])"
```

판정 규칙이 제대로 도는지 확인 (네트워크도 필요 없고 몇 초면 끝납니다):

```bash
/usr/bin/python3 tests/test_rules.py
/usr/bin/python3 tests/test_output.py
```

전체를 돌리는 것은 마지막입니다. **이건 실제로 엑셀에 줄을 추가합니다.**

```bash
cp data/후보_컴피티션_목록.xlsx "data/후보_컴피티션_목록.bak_$(date +%Y%m%d_%H%M).xlsx"  # 먼저 백업
./run.sh
```

가볍게 맛만 보려면 소스와 건수를 줄이면 됩니다.

```bash
SOURCES=devpost PAGES=1 MAX_DETAILS=5 LLM_MAX_ITEMS=0 /usr/bin/python3 main.py
```

---

## 7. 결과 엑셀 읽는 법

`data/후보_컴피티션_목록.xlsx`가 최종 산출물이고, 컬럼 15개는 기존 "확정 공지 목록"과
똑같이 맞춰져 있습니다. 처음 볼 때 헷갈리는 것만 짚으면 이렇습니다.

| 컬럼 | 알아둘 것 |
|---|---|
| **공지 상태** | `신규후보` = 조건 셋 다 자동 통과 / `신규후보(확인필요)` = 하나라도 근거를 못 찾음 / `신규후보(LLM의심)` = AI가 부적합으로 봤지만 지우지 않고 남긴 것 |
| **참가 자격** | 사이트의 응모대상 원문. AI가 돌았으면 AI가 정리한 문장 |
| 대회 일정 · 팀 인원 · 참가비 | **AI가 돌아야 채워집니다.** 전부 `확인필요`면 AI가 안 돈 것 |
| **비고** | 어느 사이트에서 왔는지 + 판정 근거 + 남은 일수(D-) |
| 담당자 | 봇은 비워둡니다. 사람이 채우는 칸 |

**`확인필요`는 "틀렸다"가 아니라 "근거를 못 찾았으니 봐 달라"는 뜻입니다.** 특히 해외
대회는 공고에 자격 요건 전문이 없는 경우가 많아 대부분 여기 들어갑니다. 공식 링크를
한 번 열어보시면 됩니다.

엑셀은 **계속 쌓이기만 합니다.** 지난 대회를 자동으로 지우는 기능은 없으니 정리는 수동입니다.

---

## 8. 이상할 때 어디를 보나

| 증상 | 먼저 볼 곳 |
|---|---|
| 수집이 0건 | 사이트 구조가 바뀌었는지. 위 6번의 한 건 받아오기를 돌려보세요 |
| 팀 인원·참가비가 전부 `확인필요` | AI가 안 돈 것. `curl -s http://127.0.0.1:11434/api/version` 으로 확인, 꺼져 있으면 `./run.sh`가 켜줍니다 |
| 판정이 이상하게 전부 통과 | 로그에 `본문 영역(.comm-desc) 없음` 경고가 있는지. 있으면 사이트 리뉴얼입니다 |
| 엑셀에 아무것도 안 늘어남 | 전부 중복 판정된 것. 로그의 `신규 N건`과 `필터 통과` 건수를 비교하세요 |
| 같은 대회가 두 줄 | 3-(3)번 참고. 제목이 한 단어 다르면 못 잡습니다 |

로그는 실행하면서 화면에 그대로 나오고, 단계마다 `=== 1) 수집 ===` 식으로 구분됩니다.
단계별 건수만 따라가도 어디서 새는지 대부분 잡힙니다.

---

## 9. 다음으로 읽을 것

- 설치·소스 목록·자동 실행 설정 → `README.md`
- 코드를 고칠 거라면 → `DEVELOPMENT.md` (특히 §9 "알려진 함정"을 먼저 보세요)
- 새 사이트를 추가하고 싶다면 → `DEVELOPMENT.md` §8. 파일 하나 만들고 `main.py`에
  한 줄 등록하면 끝입니다. 다만 **본문에 메뉴가 섞이지 않게 하는 것**만은 꼭 지켜주세요.
  이 문서 3-(1)이 그 얘기입니다.
