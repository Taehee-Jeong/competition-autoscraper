# -*- coding: utf-8 -*-
"""태희 학습 사이트의 10일 과정 내용.

명령어와 출력은 전부 이 서버에서 실제로 돌려 확인한 것만 넣는다.
안 돌아가는 예제는 배우는 사람 시간을 뺏는 것이 아니라 신뢰를 깎는다.
"""
from web.figures import (REQUEST, CRAWL, LEAK, API_VS_HTML,
                         POLITE, THREE, PIPELINE)

DAYS = [
# ------------------------------------------------------------------ 1일차
 {"title": "웹페이지는 글자다", "kind": "기초", "mins": "약 50분",
  "goal": "브라우저 없이 페이지를 받아보고, 서버가 뭐라고 대답하는지 읽는다.",
  "blocks": [
    {"svg": REQUEST, "cap": "브라우저든 파이썬이든 서버가 주는 것은 똑같다. 브라우저는 그걸 그려줄 뿐이다."},
    {"p": "브라우저로 공모전 페이지를 열면 표가 예쁘게 그려져 있고, 제목은 굵고, 마감일 옆엔 빨간 D-4가 붙어 있죠. "
          "그런데 <b>그건 브라우저가 그려준 그림</b>이에요. 서버가 실제로 보내준 건 꺾쇠투성이 글자 덩어리입니다. "
          "처음 보면 좀 당황스러운데, 익숙해지면 오히려 반가워집니다."},
    {"p": "이게 크롤링의 출발점이에요. 거창해 보이지만 <b>브라우저가 하던 일을 우리가 대신 하는 것</b>뿐입니다. "
          "서버 입장에서는 브라우저가 물어보든 파이썬이 물어보든 “이 주소 내용 주세요”라는 같은 요청일 뿐입니다."},
    {"h": "직접 받아봅시다"},
    {"code": "cd \"/home/schoaq/competition-autoscraper 2\"\n\n"
             "/usr/bin/python3 -c \"\nimport requests\n"
             "r = requests.get('https://www.wevity.com/?c=find&s=1&gbn=view&gp=1&ix=107425',\n"
             "                 headers={'User-Agent': 'Mozilla/5.0'})\n"
             "print('상태코드:', r.status_code)\nprint('받은 글자 수:', len(r.text))\n"
             "print(r.text[:200])\n\"",
     "out": "상태코드: 200\n받은 글자 수: 50353\n<!DOCTYPE html>\n<html lang=\"ko\">..."},
    {"p": "한 줄씩 보면 이렇습니다. <code>requests.get(주소)</code>가 “이 주소 주세요”라고 물어보는 부분이고, "
          "돌아온 <code>r</code>에 답이 담깁니다. <code>r.status_code</code>는 서버의 대답이 어땠는지, "
          "<code>r.text</code>는 받아온 내용 전체예요."},
    {"note": "<b>상태코드는 서버가 숫자로 하는 대답입니다.</b><br>"
             "<code>200</code> 여기 있습니다 · <code>301/302</code> 주소가 옮겨갔습니다 · "
             "<code>403</code> 너한테는 안 줍니다 · <code>404</code> 그런 거 없습니다 · "
             "<code>429</code> 너무 자주 물어봅니다 · <code>500</code> 서버가 고장났습니다.<br>"
             "크롤링하다 막히면 <b>가장 먼저 보는 게 이 숫자</b>입니다. 원인이 나한테 있는지(4xx) "
             "상대한테 있는지(5xx)가 바로 갈려요."},
    {"h": "왜 User-Agent를 넣나요"},
    {"p": "<code>User-Agent</code>는 “나는 이런 프로그램입니다”라는 자기소개입니다. "
          "안 넣으면 파이썬 기본값인 <code>python-requests/2.x</code>가 그대로 나가고, "
          "그걸 보고 “봇이네” 하며 막는 사이트가 있어요."},
    {"code": "/usr/bin/python3 -c \"\nimport requests\n"
             "for name, url in [('HackerEarth', 'https://www.hackerearth.com/challenges/'),\n"
             "                  ('Devpost',     'https://devpost.com/api/hackathons?open_state=open')]:\n"
             "    a = requests.get(url, timeout=15).status_code\n"
             "    b = requests.get(url, headers={'User-Agent':'Mozilla/5.0'}, timeout=15).status_code\n"
             "    print(f'{name:12} UA없이 {a} / UA넣고 {b}')\n\"",
     "out": "HackerEarth  UA없이 403 / UA넣고 403     ← 아예 봇을 막는 곳. 우리는 포기했습니다\n"
            "Devpost      UA없이 403 / UA넣고 403     ← 오늘 수집을 여러 번 돌려서 막혔습니다"},
    {"p": "Devpost는 <b>어제까지 잘 되던 곳</b>입니다. 오늘 수집을 네 번 돌렸더니 막혔어요. "
          "이렇게 같은 코드가 어제는 되고 오늘은 안 되는 일이 크롤링에서는 흔합니다. "
          "코드를 의심하기 전에 <b>상태코드부터 찍어보는 습관</b>이 그래서 중요해요."},
    {"warn": "브라우저인 <b>척 속이는 게 아닙니다.</b> 브라우저가 원래 보내는 정보를 우리도 보내주는 것뿐이에요. "
             "그리고 헤더를 다 넣어도 <code>403</code>이면 <b>“막았구나” 하고 물러납니다.</b> "
             "IP를 바꾸거나 우회하지 않아요. 그건 실력 문제가 아니라 태도 문제입니다."},
    {"h": "오늘 더 해볼 것",
     "p": "일부러 없는 주소(<code>…&ix=99999999</code>)를 넣어보고 상태코드가 어떻게 바뀌는지 보세요. "
          "<code>timeout=15</code>를 <code>timeout=0.001</code>로 바꾸면 어떤 오류가 나는지도 보시면, "
          "나중에 진짜 문제가 생겼을 때 오류 메시지가 눈에 들어옵니다. 미리 한 번 겪어두면 훨씬 덜 무서워요."}],
  "quiz": [{"q": "403이 왔습니다. 무엇부터 확인하시겠어요?",
            "a": "→ User-Agent 헤더. 그래도 안 되면 Referer, X-Requested-With. 그래도 403이면 물러납니다."},
           {"q": "200인데 원하는 내용이 안 보입니다. 왜일까요?",
            "a": "→ 자바스크립트로 나중에 그려지는 페이지일 수 있습니다. 5일차에서 다룹니다."},
           {"q": "어제 되던 코드가 오늘 안 됩니다. 어디부터 볼까요?",
            "a": "→ 상태코드. 403/429면 막힌 것이고, 200인데 내용이 다르면 사이트가 바뀐 것입니다."}],
  "takeaway": "웹페이지는 글자다. 서버는 숫자로 대답한다."},

# ------------------------------------------------------------------ 2일차
 {"title": "원하는 것만 뽑기", "kind": "스크래핑", "mins": "약 55분",
  "goal": "글자 더미에서 제목·마감일 같은 값만 골라낸다. 이게 '스크래핑'이다.",
  "blocks": [
    {"p": "어제 받은 게 5만 자였어요. 그 안에서 “마감일”을 눈으로 찾는 건 무리죠. "
          "다행히 HTML은 아무렇게나 쓴 글이 아니라 <b>구조가 있는 글</b>입니다."},
    {"p": "<code>&lt;li&gt;</code>는 “여기부터 여기까지가 목록 항목”, "
          "<code>&lt;div class=\"cd-area\"&gt;</code>는 “여기부터가 cd-area라는 이름의 상자”라는 표시예요. "
          "이 표시를 길잡이 삼아 원하는 조각만 집어내는 게 스크래핑입니다. "
          "<code>BeautifulSoup</code>이 그 구조를 이해해 줍니다."},
    {"code": "/usr/bin/python3 -c \"\nimport requests\nfrom bs4 import BeautifulSoup\n"
             "r = requests.get('https://www.wevity.com/?c=find&s=1&gbn=view&gp=1&ix=107425',\n"
             "                 headers={'User-Agent':'Mozilla/5.0'})\n"
             "r.encoding = r.apparent_encoding\n"
             "soup = BeautifulSoup(r.text, 'html.parser')\n\n"
             "print('제목:', soup.find('meta', property='og:title')['content'])\nprint()\n"
             "for li in soup.find('div', class_='cd-area').find_all('li')[:4]:\n"
             "    print(' ', li.get_text(' ', strip=True))\n\"",
     "out": "제목: 2026 한국수력원자력 유튜브 짧은영상 공모전 - WEVITY\n\n"
            "  분야 광고/마케팅, 영상/UCC/사진, 웹/모바일/IT, ...\n"
            "  응모대상 제한없음\n  주최/주관 한국수력원자력\n  후원/협찬"},
    {"h": "한 줄씩 뜯어보면"},
    {"p": "<code>BeautifulSoup(r.text, 'html.parser')</code> — 글자 덩어리를 구조로 바꿉니다. 이제 “찾기”가 가능해져요.<br>"
          "<code>soup.find('meta', property='og:title')</code> — 페이지가 스스로 밝힌 제목. "
          "<code>&lt;title&gt;</code>보다 깔끔한 경우가 많아 먼저 봅니다.<br>"
          "<code>soup.find('div', class_='cd-area')</code> — <b>“페이지 전체가 아니라 이 상자 안에서만 찾아”</b><br>"
          "<code>.find_all('li')</code> — 그 상자 안의 목록 항목을 전부<br>"
          "<code>.get_text(' ', strip=True)</code> — 꺾쇠를 걷어내고 글자만, 사이는 공백으로, 앞뒤 여백은 제거"},
    {"note": "<code>find</code>는 <b>첫 하나만</b>, <code>find_all</code>은 <b>조건에 맞는 전부를 리스트로</b> 돌려줍니다. "
             "<code>find</code>가 못 찾으면 <code>None</code>이 나오는데, 거기에 대고 <code>.get_text()</code>를 "
             "부르면 <code>AttributeError</code>가 나요. 이 오류를 만나면 "
             "<b>“찾는 게 없었구나”</b>로 읽으시면 됩니다 — 사이트가 바뀌었다는 신호입니다."},
    {"h": "r.encoding 은 뭔가요"},
    {"p": "한글 페이지는 인코딩을 잘못 잡으면 <code>ìëì</code> 같은 글자가 나옵니다. "
          "서버가 “나는 euc-kr이다”라고 잘못 알려주거나 아예 안 알려줄 때 생기는 일이에요. "
          "<code>r.apparent_encoding</code>은 내용을 훑어보고 실제 인코딩을 추측해 줍니다."},
    {"p": "<b>한 번 저 줄을 지우고 돌려보세요.</b> 일부러 깨뜨려보는 거예요. "
          "한글이 와장창 깨지는 걸 봐두면 나중에 다른 사이트에서 글자가 이상할 때 "
          "“아 그거구나” 하고 바로 압니다."},
    {"h": "오늘의 감각",
     "p": "스크래핑은 “정답 셀렉터를 아는 것”이 아니라 <b>“범위를 좁혀가는 것”</b>입니다. "
          "페이지 전체 → 큰 상자 → 작은 상자 → 값. 이 순서로 좁히다 보면 어디서 끊을지 감이 옵니다. "
          "처음엔 감이 안 와도 괜찮아요. 몇 번 해보면 손에 붙습니다. "
          "그리고 <b>어디서 끊느냐가 4일차의 사고를 가릅니다.</b>"}],
  "quiz": [{"q": "find와 find_all의 차이는?", "a": "→ find는 첫 하나만, find_all은 조건에 맞는 전부를 리스트로."},
           {"q": "한글이 깨져 나옵니다. 어디를 보시겠어요?", "a": "→ r.encoding. apparent_encoding으로 맞춰줍니다."},
           {"q": "'NoneType' object has no attribute 'get_text' 오류의 뜻은?",
            "a": "→ find가 못 찾아서 None이 왔다는 뜻. 사이트 구조가 바뀌었을 가능성이 큽니다."}],
  "takeaway": "찾을 범위를 먼저 좁히고, 그 안에서 값을 뽑는다."},

# ------------------------------------------------------------------ 3일차
 {"title": "목록에서 상세로 타고 들어가기", "kind": "크롤링", "mins": "약 50분",
  "goal": "주소를 손으로 넣지 않고, 목록에서 긁어모아 하나씩 방문한다. 이게 '크롤링'이다.",
  "blocks": [
    {"svg": CRAWL, "cap": "목록에서 주소를 모아(크롤링) 하나씩 방문해 값을 뽑는다(스크래핑)."},
    {"p": "지금까지는 제가 주소를 알려드렸죠. 근데 실제로는 <b>어떤 대회가 올라와 있는지 모르는 상태</b>에서 "
          "시작해요. 그래서 목록 페이지를 먼저 보고, 거기서 공고 주소를 하나씩 긁어모읍니다. "
          "오늘 배울 게 이거예요."},
    {"p": "위비티는 주소가 <code>…&gbn=view&ix=107425</code>처럼 생겼습니다. "
          "<code>ix</code> 뒤 숫자가 대회 하나를 가리키는 번호예요. 목록 페이지의 링크에서 이 번호만 모으면 됩니다."},
    {"code": "/usr/bin/python3 -c \"\nfrom scraper import wevity\n"
             "ids = wevity.list_contest_ids(pages=1)\n"
             "print(f'대회 {len(ids)}건 발견:', ids[:4])\n\n"
             "d = wevity.fetch_contest_detail(ids[0])\n"
             "print('제목  :', d['title'])\nprint('마감  :', d['deadline'])\n"
             "print('응모대상:', d['target'])\n\"",
     "out": "대회 24건 발견: ['107427', '107425', '108417', '109144']\n"
            "제목  : [국립아시아문화전당] 2026 ACC 민주·인권·평화 영상(+AI) 콘텐츠 공모전\n"
            "마감  : 2026-07-31\n응모대상: 제한없음"},
    {"note": "<b>크롤링</b>은 링크를 타고 돌아다니며 페이지를 모으는 것, "
             "<b>스크래핑</b>은 모은 페이지에서 값을 뽑는 것입니다. 방금 둘 다 했어요. "
             "실무에서는 그냥 섞어 “크롤링”이라 부르지만, "
             "<b>막혔을 때 어느 쪽이 문제인지 구분할 줄 알아야</b> 고칠 수 있습니다. "
             "수집이 0건이면 크롤링 문제, 건수는 맞는데 값이 비면 스크래핑 문제예요."},
    {"h": "왜 ID를 먼저 모으나요"},
    {"p": "목록에서 바로 상세를 받아도 되는데 굳이 번호부터 모으는 이유가 셋 있습니다.<br>"
          "① <b>중복 제거</b> — 같은 대회가 여러 페이지에 걸쳐 나올 수 있습니다<br>"
          "② <b>양 조절</b> — 24건 중 앞 5건만 받아볼 수 있습니다. 시험할 때 필수예요<br>"
          "③ <b>실패 격리</b> — 한 건이 깨져도 나머지는 계속 갑니다"},
    {"h": "코드를 열어보세요"},
    {"p": "<code>scraper/wevity.py</code>를 열면 방금 한 일이 그대로 적혀 있습니다. "
          "<code>list_contest_ids</code>가 목록에서 <code>ix=</code> 숫자를 모으고, "
          "<code>fetch_contest_detail</code>이 한 건씩 방문해요."},
    {"p": "특히 <code>list_contest_ids</code>가 <b>CSS 클래스를 안 쓴다</b>는 점을 봐주세요. "
          "링크 주소에 <code>gbn=view</code>와 <code>ix=</code>가 들어 있는지만 봅니다. "
          "사이트가 디자인을 바꿔도 주소 규칙만 그대로면 살아남는 설계예요."},
    {"h": "오늘 더 해볼 것",
     "p": "<code>pages=3</code>으로 바꿔보고 몇 건이 나오는지, 중복이 없는지 확인해 보세요. "
          "실행하는 동안 <b>몇 초 걸리는지</b> 세어보세요. 좀 답답하실 텐데, "
          "6일차에서 왜 굳이 1초씩 쉬는지 얘기하면 납득이 되실 거예요."}],
  "quiz": [{"q": "목록 3페이지를 긁으려면?", "a": "→ list_contest_ids(pages=3)"},
           {"q": "왜 ID만 먼저 모으나요?", "a": "→ 중복 제거, 양 조절, 실패 격리. 셋 다 목록 단계에서 하는 게 쌉니다."},
           {"q": "수집이 0건입니다. 크롤링 문제일까요 스크래핑 문제일까요?",
            "a": "→ 크롤링 문제. 주소를 못 모은 겁니다. 목록의 링크 규칙이 바뀐 걸 의심하세요."}],
  "takeaway": "목록 → 주소 모으기 → 하나씩 방문. 크롤링의 뼈대는 이게 전부다."},

# ------------------------------------------------------------------ 4일차
 {"title": "함정 — 메뉴가 딸려온다 ⚠", "kind": "스크래핑", "mins": "약 60분",
  "goal": "이 프로젝트가 실제로 데인 사고를 직접 재현해 본다. 오늘이 이 과정의 핵심이다.",
  "blocks": [
    {"svg": LEAK, "cap": "왼쪽 붉은 영역이 메뉴다. 페이지 전체를 퍼오면 저기 적힌 '대학생'이 공고 내용인 것처럼 섞인다."},
    {"p": "공모전 페이지에는 공고만 있는 게 아닙니다. 왼쪽에 카테고리 메뉴, 위에 검색창, 아래에 회사 정보가 있죠. "
          "사람 눈에는 “저건 메뉴고 이건 공고”가 당연히 구분되잖아요. "
          "그런데 <b>글자만 퍼오면 그 구분이 사라집니다.</b> 말로 들으면 뻔한데 막상 당하면 못 알아채요. "
          "직접 세어봅시다."},
    {"code": "/usr/bin/python3 -c \"\nimport requests\nfrom bs4 import BeautifulSoup\n"
             "r = requests.get('https://www.wevity.com/?c=find&s=1&gbn=view&gp=1&ix=107425',\n"
             "                 headers={'User-Agent':'Mozilla/5.0'})\n"
             "r.encoding = r.apparent_encoding\n"
             "soup = BeautifulSoup(r.text, 'html.parser')\n\n"
             "전체 = soup.get_text()\n"
             "본문만 = soup.find('div', class_='comm-desc').get_text()\n\n"
             "for 낱말 in ['대학생', '청소년', '누구나', '전략']:\n"
             "    print(f'{낱말:5} 전체페이지 {전체.count(낱말)}번 / 공고본문 {본문만.count(낱말)}번')\n\"",
     "out": "대학생   전체페이지 1번 / 공고본문 0번\n청소년   전체페이지 1번 / 공고본문 0번\n"
            "누구나   전체페이지 1번 / 공고본문 0번\n전략     전체페이지 2번 / 공고본문 0번"},
    {"p": "<b>오른쪽이 전부 0입니다.</b> 이 공고 본문에는 “대학생”이라는 말이 한 번도 안 나와요. "
          "그런데 페이지 전체를 보면 나옵니다. 어디서 왔을까요?"},
    {"p": "왼쪽 메뉴의 <code>응시대상자: 전체 / 제한없음 / 일반인 / 대학생 / 청소년</code>입니다. "
          "그리고 <code>전략</code>은 <code>공모전전략</code>이라는 메뉴 항목에서 왔어요."},
    {"h": "이게 무엇을 망가뜨렸나"},
    {"p": "이 봇은 “글에 <b>대학생</b>이 있으면 대학생이 참가할 수 있는 대회”라고 판단합니다. "
          "그런데 저 메뉴가 <b>모든 페이지에</b> 있으니, 결과가 이렇게 됐습니다."},
    {"out": "실제 5건을 뽑아 확인한 결과\n"
            "  대학생 5/5건 매치 · 누구나 5/5건 · 제한없음 5/5건 · 전략 5/5건\n\n"
            "→ 참가 자격을 한 글자도 안 밝힌 공고까지 전부 '대학생 참가 가능'"},
    {"warn": "<code>전략</code>은 더 나빴습니다. 이 프로젝트에는 판정 규칙을 스스로 고치는 기능이 있는데, "
             "그게 <code>전략</code>을 <b>“팀 참가 가능 신호”로 학습해버렸습니다.</b> "
             "메뉴에 있는 단어라 모든 공고에서 발견되니, 규칙 입장에선 “아주 잘 맞는 신호”처럼 보였던 거예요. "
             "그 결과 개인 참가만 되는 대회도 팀 가능으로 표시됐습니다."},
    {"h": "고친 방법과 지키는 방법"},
    {"p": "고친 건 한 줄입니다. <code>soup.get_text()</code>(전체) 대신 "
          "<code>soup.find('div', class_='comm-desc')</code>(공고 본문만). "
          "그 결과 본문이 2,600~4,400자에서 <b>1,000자 안팎</b>으로 줄었습니다. 줄어든 만큼이 전부 메뉴였어요."},
    {"code": "/usr/bin/python3 tests/test_wevity_live.py",
     "out": "수집 5건 / 본문 평균 1011자\n필드 채워짐: {'deadline': 5, 'target': 5, ...}\n\n"
            "OK: 본문에 메뉴 텍스트 없음, 필드 전건 채워짐"},
    {"note": "이 테스트는 위비티에서 실제로 5건을 받아 본문에 메뉴 낱말(<code>공모전전략</code>, "
             "<code>메일링 신청</code>, <code>응시대상자</code>…)이 섞였는지 검사합니다. "
             "<b>사고를 고친 뒤엔 테스트를 남긴다</b> — 두 번 안 당하는 유일한 방법이에요."},
    {"h": "그런데 클래스를 쓰면 사이트 바뀔 때 깨지지 않나요"},
    {"p": "좋은 질문이고, 맞습니다. 그래서 <b>못 찾으면 조용히 틀리는 대신 경고를 남기게</b> 했어요. "
          "로그에 <code>본문 영역(.comm-desc) 없음</code>이 뜨면 위비티가 리뉴얼했다는 신호입니다. "
          "정확도와 견고함 중에 골라야 할 때는, <b>틀린 답을 조용히 내는 쪽이 항상 더 나쁩니다.</b><br>"
          "오늘이 열흘 중 제일 중요한 날이에요. 여기만 확실히 잡고 가시면 나머지는 수월합니다."}],
  "quiz": [{"q": "공고 본문에 '대학생'이 없는데 봇이 대학생 가능이라 합니다. 어디를 보시겠어요?",
            "a": "→ 페이지 전체 텍스트를 쓰고 있는지. 메뉴·사이드바가 섞였을 겁니다."},
           {"q": "새 사이트를 붙일 때 가장 먼저 확인할 것은?", "a": "→ 공고 본문만 담긴 요소가 어디인지."},
           {"q": "본문 요소를 못 찾으면 전체 텍스트를 넣어도 될까요?",
            "a": "→ 안 됩니다. 경고를 남기고 제목·분야·기간 요약만 넣어 '확인필요'로 남깁니다."}],
  "takeaway": "페이지 전체를 퍼오지 않는다. 공고 본문만 잘라낸다."},

# ------------------------------------------------------------------ 5일차
 {"title": "API가 있으면 API를 써라", "kind": "공통", "mins": "약 55분",
  "goal": "HTML을 뜯는 건 최후의 수단임을 안다. 숨은 데이터 통로를 찾는 법을 익힌다.",
  "blocks": [
    {"svg": API_VS_HTML, "cap": "같은 정보인데 오른쪽은 이름표가 붙어 온다."},
    {"p": "요즘 사이트는 화면을 그릴 때 HTML에 내용을 다 박아 보내지 않습니다. "
          "빈 껍데기를 먼저 주고, 그다음 <b>자바스크립트가 데이터를 따로 받아와</b> 화면을 채워요. "
          "그 “따로 받아오는 통로”가 API입니다."},
    {"p": "여기서 많이들 포기해요. 페이지를 받았는데 목록이 텅 비어 있으니까요. "
          "저도 처음엔 “아 이 사이트는 안 되는구나” 하고 넘겼습니다. "
          "그런데 <b>없는 게 아니라 다른 주소에 있는 것</b>이었어요. 그리고 그 주소는 우리도 부를 수 있습니다."},
    {"code": "/usr/bin/python3 -c \"\nimport requests\n"
             "r = requests.get('https://app.dacon.io/api/v1/competition/list',\n"
             "                 params={'offset':0, 'limit':3},\n"
             "                 headers={'User-Agent':'Mozilla/5.0', 'Accept':'application/json'})\n"
             "print('상태코드:', r.status_code)\n"
             "for c in r.json()['data'][:3]:\n"
             "    print(f\\\"  {c['name'][:38]:<40} 마감 {c['period_end'][:10]}  상금 {c['prize']}만원\\\")\n\"",
     "out": "상태코드: 200\n  제3회 풍력발전량 예측 AI 경진대회 - BARAM 2026    마감 2026-08-14  상금 2000만원\n"
            "  2026 Samsung Collegiate Programming Ch    마감 2026-07-12  상금 7000만원"},
    {"p": "차이가 보이시나요. <code>r.json()</code> 한 번이면 파이썬 딕셔너리가 되고, "
          "<code>c['name']</code>으로 값을 바로 꺼냅니다. 꺾쇠를 뜯을 일도, "
          "메뉴가 섞일 걱정도 없어요. 4일차의 사고가 <b>구조적으로 일어날 수 없습니다.</b>"},
    {"h": "API 찾는 법 — 순서대로"},
    {"p": "① 브라우저로 목록 페이지를 엽니다<br>"
          "② <code>F12</code>를 눌러 개발자 도구를 엽니다<br>"
          "③ <b>Network</b> 탭 → <code>Fetch/XHR</code> 필터를 누릅니다<br>"
          "④ 페이지를 새로고침하거나 “다음 페이지”를 누릅니다<br>"
          "⑤ 목록 데이터가 담긴 요청을 찾습니다 (보통 <code>/api/</code>, <code>.json</code>, <code>/graphql</code>)<br>"
          "⑥ 그 요청을 눌러 <b>Response</b> 탭에서 내용을 확인합니다<br>"
          "⑦ <b>Headers</b> 탭에서 주소와 파라미터를 그대로 베껴 씁니다"},
    {"note": "HTML 안에 숨어 있는 경우도 있습니다. 페이지 소스에서 "
             "<code>__NEXT_DATA__</code>, <code>window.__INITIAL_STATE__</code>, "
             "<code>&lt;script type=\"application/ld+json\"&gt;</code>를 찾아보세요. "
             "화면에 안 보이지만 데이터가 통째로 들어 있는 경우가 많습니다. "
             "이 프로젝트의 <b>캠퍼스픽</b>이 그렇게 붙었어요."},
    {"h": "실제로 있었던 일"},
    {"p": "이 프로젝트 문서에는 원래 이렇게 적혀 있었습니다 — "
          "<b>“씽굿·링커리어·MLH는 목록이 자바스크립트로 렌더링되어 단순 requests로는 수집 불가”.</b> "
          "그런데 확인해보니 전부 내부 API를 쓰고 있었고, 헤드리스 브라우저 없이 그냥 붙었습니다."},
    {"p": "지금 16개 소스 중 <b>7곳이 API 방식</b>입니다(씽굿·링커리어·데이콘·Devpost·Unstop·MLH·Zindi). "
          "“JS라서 안 된다”는 결론은 <b>API를 안 찾아봤다는 뜻인 경우가 많습니다.</b>"},
    {"h": "오늘 더 해볼 것",
     "p": "<code>limit</code>을 10으로, <code>offset</code>을 3으로 바꿔보세요. "
          "파라미터를 어떻게 조합하면 원하는 만큼 가져오는지 감이 잡힙니다. "
          "그다음 <code>scraper/dacon.py</code>를 열어 실제로 어떻게 쓰는지 비교해 보세요. "
          "오늘 배운 게 그대로 들어 있어서 반가우실 거예요."}],
  "quiz": [{"q": "목록이 HTML에 안 보입니다. 포기하기 전에?", "a": "→ F12 → Network → Fetch/XHR 에서 내부 API 찾기."},
           {"q": "API가 HTML 파싱보다 나은 이유 두 가지는?",
            "a": "→ 값이 이름표와 함께 바로 오고, 디자인이 바뀌어도 안 깨집니다. 메뉴가 섞일 일도 없습니다."},
           {"q": "Network 탭에 아무것도 안 보입니다. 다음은?",
            "a": "→ 페이지 소스에서 __NEXT_DATA__ / ld+json 을 찾아보세요. HTML 안에 숨어 있을 수 있습니다."}],
  "takeaway": "HTML을 뜯기 전에 API를 먼저 찾는다."},

# ------------------------------------------------------------------ 6일차
 {"title": "예의와 규칙", "kind": "예의", "mins": "약 45분",
  "goal": "기술적으로 되는 것과 해도 되는 것을 구분한다. 이건 실력이 아니라 태도의 문제다.",
  "blocks": [
    {"svg": POLITE, "cap": "새 사이트를 붙일 때의 순서. 첫 관문에서 막히면 거기서 끝이다."},
    {"p": "오늘은 코드가 거의 없어요. 대신 제일 중요한 얘기입니다.<br>"
          "크롤링은 <b>남의 서버에 요청을 보내는 일</b>이에요. 내 컴퓨터에서 돌지만 부담은 상대가 집니다. "
          "이 감각이 없으면 실력이 늘수록 민폐가 돼요. 반대로 이것만 지키면 크게 사고 칠 일이 없습니다."},
    {"h": "robots.txt — 사이트 주인의 요청"},
    {"p": "주소 뒤에 <code>/robots.txt</code>를 붙이면 “여기는 프로그램이 긁어가지 말아 달라”는 안내가 나옵니다. "
          "법적 강제력은 없지만 <b>지키는 게 기본</b>이에요."},
    {"code": "/usr/bin/python3 -c \"\nimport requests\n"
             "for site in ['https://www.wevity.com', 'https://news.google.com', 'https://search.naver.com']:\n"
             "    t = requests.get(site + '/robots.txt', headers={'User-Agent':'Mozilla/5.0'}, timeout=10).text\n"
             "    print(f'{site:32}  전면금지 여부:', 'Disallow: /' in t)\n\"",
     "out": "https://www.wevity.com            전면금지 여부: False\n"
            "https://news.google.com           전면금지 여부: True\n"
            "https://search.naver.com          전면금지 여부: True"},
    {"p": "읽는 법은 간단합니다. <code>User-agent: *</code>는 “모든 프로그램에게”, "
          "<code>Disallow: /경로</code>는 “이 경로는 안 됩니다”, <code>Allow:</code>는 예외로 허용."},
    {"warn": "네이버 검색과 구글 뉴스는 <b>전면 금지</b>입니다. 실제로 이 프로젝트에서 "
             "“뉴스로 공모전을 더 찾아보자”는 시도를 했다가 <b>이것 때문에 접었습니다.</b> "
             "구글 뉴스는 한술 더 떠 AI 봇 이름을 하나하나 적어 막아뒀어요. "
             "<b>“막혀 있으니 뚫자”가 아니라 “금지면 안 한다”</b>가 원칙입니다."},
    {"h": "요청 사이에 쉬기"},
    {"p": "이 프로젝트는 요청마다 1초씩 쉽니다(<code>scraper/wevity.py</code>의 <code>time.sleep</code>). "
          "60건을 받으면 그것만으로 1분이 더 걸려요. 답답하지만 이유가 있습니다."},
    {"note": "<b>오늘 실제로 겪은 일:</b> 하루에 수집을 네 번 돌렸더니 "
             "<b>Devpost가 403을 주기 시작했습니다.</b> 1일차 실습에서 Devpost가 403이었던 게 바로 이것이에요. "
             "한 번 막히면 며칠 기다리거나 간격을 더 늘려야 하고, 그동안 그 소스는 못 씁니다. "
             "<b>1초 아끼려다 일주일을 잃는 셈입니다.</b>"},
    {"h": "그 밖에 지키는 것"},
    {"p": "· <b>로그인이 필요한 곳은 건드리지 않습니다.</b> 지금 16곳 전부 공개된 것만 가져옵니다<br>"
          "· <b>수집한 데이터는 교내 공지용으로만</b> 씁니다. 재배포하거나 서비스로 만들지 않습니다<br>"
          "· <b>이용약관을 봅니다.</b> robots.txt가 열려 있어도 약관이 금지하면 안 합니다<br>"
          "· <b>개인정보는 아예 안 건드립니다.</b> 우리가 모으는 건 공고이지 사람이 아닙니다"},
    {"h": "오늘 더 해볼 것",
     "p": "붙이고 싶은 사이트를 하나 떠올리고 그 <code>robots.txt</code>를 직접 열어보세요. "
          "무엇이 허용되고 무엇이 금지인지 읽어보세요. 9일차에 새 소스를 붙일 때 첫 단계를 미리 해두는 셈이에요."}],
  "quiz": [{"q": "robots.txt에 Disallow: / 가 있습니다. 어떻게 하시겠어요?", "a": "→ 안 합니다. 다른 통로를 찾습니다."},
           {"q": "빨리 긁고 싶어 sleep을 지웠습니다. 무슨 일이 생길까요?",
            "a": "→ 상대 서버에 부담이 가고, 차단당하면 며칠간 그 소스를 통째로 못 씁니다."},
           {"q": "robots.txt는 열려 있는데 이용약관이 크롤링을 금지합니다. 어떻게 할까요?",
            "a": "→ 안 합니다. robots.txt는 기술적 안내일 뿐, 약관이 더 위입니다."}],
  "takeaway": "되는 것과 해도 되는 것은 다르다."},

# ------------------------------------------------------------------ 7일차
 {"title": "제각각인 값을 하나로 맞추기", "kind": "스크래핑", "mins": "약 55분",
  "goal": "사이트마다 다른 날짜 표기를 하나의 형식으로 바꾼다. 여기서 정규식과 친해진다.",
  "blocks": [
    {"p": "값을 뽑았다고 끝이 아니에요. <b>사이트마다 쓰는 모양이 제각각이거든요.</b> "
          "같은 “마감일”인데 이렇게 씁니다. 보시면 좀 어이없으실 거예요."},
    {"out": "2026-07-30                       위비티 (라벨 필드)\n"
            "2026. 6. 1.(월)~7. 30.(목) 17:00    공고 본문 안\n"
            "Jul 21 - Aug 25, 2026             Devpost (영문)\n"
            "2026-08-14 09:59:59               데이콘 (시각까지)"},
    {"p": "이대로는 “마감까지 2주 남았나?”를 계산할 수 없습니다. "
          "전부 <code>2026-07-30</code> 하나로 맞춰야 비교가 되죠. "
          "<b>이 정리 작업이 스크래핑의 절반</b>입니다. 값을 뽑는 것보다 오래 걸릴 때도 많아요."},
    {"code": "/usr/bin/python3 -c \"\nfrom scraper.devpost import _parse_end_date\n"
             "from scraper.filters import _parse_date, check_deadline\n"
             "from datetime import date\n\n"
             "print(_parse_end_date('Jul 21 - Aug 25, 2026'))\n"
             "print(_parse_date('2026.08.14'))\n\n"
             "print(check_deadline({'deadline':'2026-12-31'}, today=date(2026,7,26)))\n"
             "print(check_deadline({'deadline':'2026-07-30'}, today=date(2026,7,26)))\n"
             "print(check_deadline({'deadline':None},        today=date(2026,7,26)))\n\"",
     "out": "2026-08-25\n2026-08-14\n('통과', 158)\n('탈락', 4)\n('확인필요', None)"},
    {"p": "앞의 둘은 형식 변환입니다. 영문 <code>Aug 25, 2026</code>도, 점을 쓴 <code>2026.08.14</code>도 "
          "같은 모양으로 나왔죠. 뒤의 셋은 그 값으로 판정한 결과입니다."},
    {"note": "<b>마지막 줄을 보세요.</b> 마감일을 못 읽었을 때 <b>탈락이 아니라 확인필요</b>입니다. "
             "정보가 없다고 버리지 않아요. 이게 이 프로젝트의 원칙입니다 — <b>놓치느니 남긴다.</b> "
             "내일 이 얘기를 더 자세히 합니다."},
    {"h": "정규식은 조금씩"},
    {"p": "<code>scraper/devpost.py</code>의 <code>_parse_end_date</code>를 열어보세요. 이렇게 생겼습니다."},
    {"code": "([A-Z][a-z]{2})[a-z]*\\s+(\\d{1,2}),?\\s+(\\d{4})"},
    {"p": "딱 봐도 무섭게 생겼죠. 근데 조각내서 보면 별거 아니에요.<br>"
          "<code>[A-Z][a-z]{2}</code> — 대문자 하나 + 소문자 둘 → <code>Jul</code>, <code>Aug</code><br>"
          "<code>[a-z]*</code> — 뒤에 소문자가 더 와도 됨 → <code>July</code>도 잡힘<br>"
          "<code>\\s+</code> — 공백 하나 이상<br>"
          "<code>\\d{1,2}</code> — 숫자 1~2자리 → 날짜 <code>25</code><br>"
          "<code>,?</code> — 쉼표가 있어도 없어도 됨<br>"
          "<code>\\d{4}</code> — 숫자 4자리 → 연도 <code>2026</code>"},
    {"note": "정규식은 <b>한 번에 다 외우는 게 아닙니다.</b> 필요할 때 한 조각씩 읽으면 돼요. "
             "자주 쓰는 건 넷뿐이에요 — <code>\\d</code>(숫자), <code>\\s</code>(공백), "
             "<code>+</code>(하나 이상), <code>?</code>(있어도 없어도). "
             "이 넷만 알아도 여기 나오는 건 다 읽힙니다."},
    {"h": "값이 틀리면 조용히 틀린다"},
    {"p": "이 프로젝트에서 실제로 있었던 일입니다. 라벨과 값을 나누는 코드가 "
          "<b>값이 빈 항목</b>(<code>후원/협찬</code>)을 만나면 줄바꿈을 삼키고 "
          "<b>다음 줄 전체를 자기 값으로 가져갔습니다.</b> 그래서 <code>접수기간</code>이 통째로 사라졌어요."},
    {"p": "아무도 몰랐습니다. 오류가 안 나거든요. 마감일이 그냥 “확인필요”로 나올 뿐이었죠. "
          "<b>스크래핑 버그는 대부분 이렇게 조용합니다.</b> 그래서 뽑은 값을 눈으로 한 번 찍어보는 습관이 중요해요. "
          "귀찮지만 이게 나중에 몇 시간을 아껴줍니다."}],
  "quiz": [{"q": "마감일을 못 읽으면 어떻게 처리하나요?", "a": "→ 확인필요. 버리지 않고 사람이 보게 남깁니다."},
           {"q": "\\d{4} 는 무슨 뜻인가요?", "a": "→ 숫자 4자리. 연도를 찾을 때 씁니다."},
           {"q": "스크래핑 버그가 위험한 이유는?",
            "a": "→ 오류가 안 나고 조용히 틀린 값을 냅니다. 값을 직접 찍어봐야 발견됩니다."}],
  "takeaway": "값의 모양은 소스마다 다르다. 안에서 하나로 맞춰 넘긴다."},

# ------------------------------------------------------------------ 8일차
 {"title": "판정 규칙 읽기", "kind": "활용", "mins": "약 50분",
  "goal": "수집한 공고를 어떤 기준으로 거르는지 이해하고, 왜 세 값인지 안다.",
  "blocks": [
    {"svg": THREE, "cap": "'안 된다'는 근거를 먼저 본다. 근거가 없으면 버리지 않고 사람에게 넘긴다."},
    {"p": "여기까지 오셨으면 값은 다 모은 거예요. 남은 건 <b>우리 기준에 맞는지 판단하는 일</b>입니다. 기준은 셋이에요. "
          "① 재학생 참가 가능 ② 팀(2인 이상) 참가 가능 ③ 마감까지 2주 이상."},
    {"code": "/usr/bin/python3 -c \"\nfrom scraper.filters import check_student, check_team\n"
             "for 본문 in ['대학생 및 휴학생, 팀 단위(2~5인) 접수',\n"
             "             '초·중·고 재학생만 참가 가능',\n"
             "             '개인 참가만 가능하며 팀 접수는 받지 않습니다',\n"
             "             '출품작은 A4 5매 이내로 제출합니다']:\n"
             "    s = check_student({'body': 본문}); t = check_team({'body': 본문})\n"
             "    print(f'{본문[:30]:<32} 재학생={s} 팀={t}')\n\"",
     "out": "대학생 및 휴학생, 팀 단위(2~5인) 접수      재학생=통과   팀=통과\n"
            "초·중·고 재학생만 참가 가능              재학생=탈락   팀=확인필요\n"
            "개인 참가만 가능하며 팀 접수는 받지 않습니다   재학생=확인필요 팀=탈락\n"
            "출품작은 A4 5매 이내로 제출합니다          재학생=확인필요 팀=확인필요"},
    {"h": "왜 두 값이 아니라 세 값인가"},
    {"p": "마지막 줄이 핵심입니다. 참가 자격 얘기가 아예 없으니 <b>확인필요</b>. "
          "여기서 “안 된다”고 단정하면 멀쩡한 대회를 버리게 되고, “된다”고 하면 "
          "4일차의 사고가 반복됩니다. <b>모른다는 것과 아니라는 것은 다릅니다.</b>"},
    {"p": "그래서 판정 값이 셋이에요.<br>"
          "<b>통과</b> — 된다는 근거를 찾았다<br>"
          "<b>탈락</b> — 안 된다는 근거를 찾았다 (목록에서 뺀다)<br>"
          "<b>확인필요</b> — 판단할 근거가 없다 (버리지 않고 사람에게 넘긴다)"},
    {"note": "그래서 <b>엑셀에 “확인필요”가 많은 건 고장이 아닙니다.</b> "
             "특히 해외 대회는 공고에 자격 요건 전문이 없는 경우가 많아 대부분 여기 들어가요. "
             "공식 링크를 한 번 열어보시면 됩니다."},
    {"h": "부정문의 함정"},
    {"p": "세 번째 줄도 눈여겨보세요. <code>팀 접수는 받지 않습니다</code>라는 <b>부정문</b> 안에 "
          "“팀 접수”라는 말이 들어 있습니다."},
    {"warn": "예전 코드는 이렇게 판단했습니다 — “배제 신호가 있어도 <b>긍정 신호가 같이 있으면</b> 통과”. "
             "그래서 <code>팀 참가 불가</code>라고 적힌 공고가 <b>팀 가능</b>으로 나왔어요. "
             "부정문 안의 “팀 참가”를 긍정 신호로 읽은 거죠. "
             "지금은 <b>배제 문구가 무조건 우선</b>합니다. 사람의 상식과 같은 순서예요."},
    {"h": "규칙은 어디에 있나"},
    {"p": "<code>scraper/filters.py</code> 위쪽의 네 목록입니다. "
          "<code>STUDENT_NEG</code>(재학생 배제 신호), <code>STUDENT_POS</code>(재학생 허용 신호), "
          "<code>TEAM_NEG</code>, <code>TEAM_POS</code>. 그냥 정규식을 나열해둔 리스트예요. "
          "새 표현을 발견하면 여기 한 줄 추가하면 됩니다."},
    {"code": "/usr/bin/python3 tests/test_rules.py",
     "out": "  PASS  test_team_negative_wins_over_positive\n  PASS  test_golden_set_no_regression\n  ...\n\n10 passed, 0 failed"},
    {"note": "규칙을 고쳤으면 <b>반드시 이 테스트를 돌리세요.</b> 12건짜리 정답 세트로 회귀를 검사합니다. "
             "한 표현을 고치려다 다른 걸 망가뜨리는 일이 정말 자주 생깁니다. 저도 오늘만 두 번 그랬어요."}],
  "quiz": [{"q": "확인필요는 오류인가요?", "a": "→ 아닙니다. 근거를 못 찾았으니 사람이 봐 달라는 뜻입니다."},
           {"q": "'팀 참가 불가'가 '팀 가능'으로 읽히지 않으려면?", "a": "→ 배제(NEG) 문구를 긍정(POS)보다 먼저 확인해야 합니다."},
           {"q": "규칙에 새 표현을 추가한 뒤 할 일은?", "a": "→ tests/test_rules.py 실행. 다른 판정이 망가지지 않았는지 봅니다."}],
  "takeaway": "모른다는 것과 아니라는 것은 다르다."},

# ------------------------------------------------------------------ 9일차
 {"title": "직접 붙여보기", "kind": "종합", "mins": "약 60분",
  "goal": "새 공모전 사이트 하나를 골라 수집기를 만들어 본다. 오늘은 끝까지 못 가도 괜찮다.",
  "blocks": [
    {"p": "드디어 직접 만들어보는 날이에요. 여덟 날 배운 걸 전부 씁니다. "
          "순서는 이미 다 아시는 거라 새로 외울 건 없어요."},
    {"out": "1. robots.txt 확인            → 금지면 거기서 끝           (6일차)\n"
            "2. F12 → Network → Fetch/XHR → API부터 찾기              (5일차)\n"
            "3. 없으면 HTML에서 본문 요소 찾기                          (4일차 ⚠)\n"
            "4. 목록에서 주소 모으기 → 상세 방문                        (3일차)\n"
            "5. 값 뽑고 날짜 형식 맞추기                                (2·7일차)\n"
            "6. scraper/ 에 파일 하나 만들고 main.py에 등록\n"
            "7. 소스 하나만 켜서 시험"},
    {"h": "본떠 쓸 예제"},
    {"p": "맨땅에서 시작하지 마세요. 그건 저도 안 합니다. <code>scraper/devpost.py</code>(API형, 94줄)와 "
          "<code>scraper/wevity.py</code>(HTML형, 150줄) 중 <b>닮은 쪽을 복사해서 고치는 게</b> 가장 빠릅니다. "
          "16개 소스 전부 그렇게 만들어졌어요."},
    {"h": "반환해야 하는 모양"},
    {"p": "어떤 사이트를 붙이든 <b>이 모양의 딕셔너리 리스트</b>를 돌려주면 됩니다. "
          "그 뒤 판정·보강·엑셀은 이미 만들어진 게 알아서 처리해요."},
    {"code": "{\n  \"source\": \"내사이트\", \"ix\": \"고유ID\",\n"
             "  \"url\": \"공고 주소\", \"homepage\": \"공식 홈페이지\",\n"
             "  \"title\": \"대회명\", \"field\": \"분야\", \"target\": \"응모대상 원문\",\n"
             "  \"host\": \"주최\", \"period\": \"접수기간 원문\", \"prize\": \"상금\",\n"
             "  \"deadline\": \"2026-09-30\",   # YYYY-MM-DD 또는 None\n"
             "  \"body\": \"공고 본문만!\",       # 메뉴 섞이면 안 됨\n"
             "  \"lang\": \"ko\",\n}"},
    {"h": "등록하기"},
    {"p": "<code>main.py</code>의 <code>SOURCE_REGISTRY</code>에 한 줄, "
          "<code>SOURCE_INFO</code>에 한 줄. 등록하면 <b>검수 웹의 수집 실행 화면에도 자동으로 체크박스가 생깁니다.</b> "
          "둘이 어긋나면 프로그램이 시작할 때 바로 알려줘요."},
    {"h": "시험하기"},
    {"code": "SOURCES=내소스 PAGES=1 MAX_DETAILS=5 /usr/bin/python3 main.py"},
    {"warn": "<b>이건 실제 엑셀에 줄을 추가합니다.</b> 시험 전에 백업하세요.<br>"
             "<code>cp data/후보_컴피티션_목록.xlsx \"data/백업_$(date +%m%d_%H%M).xlsx\"</code>"},
    {"note": "5건만 받아서 <b>body를 눈으로 확인하세요.</b> 메뉴가 섞였는지, 마감일이 제대로 나왔는지. "
             "여기서 대충 넘어가면 4일차의 사고가 새 사이트에서 반복됩니다. "
             "자세한 절차는 <code>DEVELOPMENT.md</code> §8에 있어요."},
    {"h": "막히면"},
    {"p": "· 목록이 0건 → 크롤링 문제. 링크 규칙을 다시 보세요 (3일차)<br>"
          "· 건수는 맞는데 값이 빔 → 스크래핑 문제. 셀렉터를 보세요 (2일차)<br>"
          "· 마감일만 안 나옴 → 날짜 형식이 예상과 다릅니다 (7일차)<br>"
          "· 403 → 헤더를 맞춰보고, 그래도 안 되면 포기 (1·6일차)<br><br>"
          "<b>오늘 끝까지 못 가셔도 전혀 괜찮아요.</b> 처음 붙이는 사이트는 원래 하루 넘게 걸립니다. "
          "어디서 막혔는지만 적어두시면 같이 보면 돼요."}],
  "quiz": [{"q": "새 소스에서 가장 지키기 어려운 원칙은?",
            "a": "→ body에 공고 본문만 담기. 사이트마다 구조가 달라 매번 새로 찾아야 합니다."},
           {"q": "본문 요소를 못 찾으면 전체 텍스트를 넣어도 될까요?",
            "a": "→ 안 됩니다. 차라리 제목·분야·기간 요약을 넣고 확인필요로 남깁니다."},
           {"q": "수집은 되는데 마감일만 전부 비어 있습니다. 어디를 볼까요?",
            "a": "→ 날짜 파싱. 그 사이트의 표기가 예상 형식과 다를 겁니다 (7일차)."}],
  "takeaway": "붙이는 방법은 늘 같다. 어려운 건 본문을 찾는 일뿐이다."},

# ------------------------------------------------------------------ 10일차
 {"title": "돌아보기와 다음", "kind": "정리", "mins": "약 40분",
  "goal": "열흘을 정리하고, 이 프로젝트가 어디까지 왔고 무엇이 남았는지 본다.",
  "blocks": [
    {"h": "열흘 동안 배운 것"},
    {"out": "1일  웹페이지는 글자다, 서버는 숫자로 답한다        [기초]\n"
            "2일  범위를 좁히고 값을 뽑는다                   [스크래핑]\n"
            "3일  목록 → 주소 → 상세                        [크롤링]\n"
            "4일  ⚠ 페이지 전체를 퍼오지 않는다               [스크래핑]\n"
            "5일  HTML 뜯기 전에 API를 찾는다                [공통]\n"
            "6일  금지면 안 한다, 1초씩 쉰다                  [예의]\n"
            "7일  값의 모양을 하나로 맞춘다                   [스크래핑]\n"
            "8일  모른다와 아니다는 다르다                    [활용]\n"
            "9일  새 소스 붙이기                            [종합]\n"
            "10일 정리"},
    {"svg": PIPELINE, "cap": "수집부터 검수까지. 사람이 누른 판단이 다시 규칙을 가르친다."},
    {"h": "지금 이 봇의 상태"},
    {"p": "열흘 하셨으면 이제 이 봇이 뭘 하는지 다 아시는 거예요. "
          "소스 <b>16곳</b>, 후보 <b>260여 건</b>, 주 1회 자동 수집. "
          "검수 웹에서 확정·기각을 누르면 그 판단이 판정 규칙 학습에 쓰입니다. "
          "특히 <b>기각 사유</b>가 중요해요 — “재학생 불가”로 기각하면 그게 규칙에게 "
          "“이런 건 걸러라”라고 가르치는 예시가 됩니다."},
    {"h": "아직 안 풀린 문제 (같이 고민해볼 것)"},
    {"out": "· 국내 사이트 5곳이 같은 공고를 올려 중복이 남는다\n"
            "  (제목이 '대학가요제'/'대학 가요제'처럼 한 글자씩 다름)\n\n"
            "· 로컬 AI의 판정이 아직 못 미덥다\n"
            "  (초등학생 전용 공모전에 '대학생 참가 가능'이라 지어낸 적이 있다)\n\n"
            "· 씽굿엔 접수중이 864건인데 우리는 30건만 가져온다"},
    {"note": "마지막 줄이 재미있습니다. “더 많이 찾기”의 답이 꼭 똑똑한 AI는 아니에요. "
             "실제로 검색·뉴스로 자동 발견을 시도했다가 robots 금지와 정확도 문제로 접었고, "
             "<b>이미 있는 통로를 넓히는 게 훨씬 확실했습니다.</b> "
             "새 기술을 붙이기 전에 <b>지금 것을 다 쓰고 있는지</b> 먼저 보는 습관이 중요합니다."},
    {"h": "더 읽을 것"},
    {"p": "<code>입문.md</code> — 개념과 이 봇의 설계 배경<br>"
          "<code>태희_크롤링_실습.md</code> — 이 과정의 요약본 (터미널에서 보기 좋음)<br>"
          "<code>DEVELOPMENT.md</code> — 내부 구조와 함정 모음. <b>특히 §9는 사고 기록집</b>입니다<br>"
          "<code>scraper/wevity.py</code>, <code>scraper/devpost.py</code> — 두 방식의 본보기"},
    {"h": "마지막으로"},
    {"p": "크롤링에서 어려운 건 코드가 아니에요. <b>사이트가 계속 바뀐다는 것</b>과 "
          "<b>틀렸는데 오류가 안 난다는 것</b>입니다. 그래서 값을 눈으로 찍어보고, "
          "고친 뒤엔 테스트를 남기고, 확신이 없으면 “확인필요”로 두는 거예요.<br><br>"
          "열흘 동안 고생 많으셨습니다. 여기 적힌 사고들 대부분이 저희가 실제로 당한 것들이라, "
          "태희 씨는 적어도 같은 함정은 피해 가실 거예요."}],
  "quiz": [{"q": "이 과정에서 가장 중요한 하루를 꼽는다면?",
            "a": "→ 4일차. 본문만 잘라내기. 나머지 전부가 그 위에 얹힙니다."},
           {"q": "새 사이트를 붙일 때 첫 단계는?", "a": "→ robots.txt 확인. 금지면 시작도 안 합니다."},
           {"q": "크롤링에서 가장 위험한 버그는?",
            "a": "→ 오류 없이 조용히 틀린 값을 내는 것. 그래서 값을 직접 찍어보고 테스트를 남깁니다."}],
  "takeaway": "도구는 계속 바뀐다. 남의 서버를 존중하는 태도만 안 바뀐다."},
]
