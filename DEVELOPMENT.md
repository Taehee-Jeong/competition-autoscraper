# 개발 문서 (코드를 만질 사람용)

이 문서는 `competition-autoscraper`를 **처음 열어본 개발자**가 30분 안에 구조를 파악하고
직접 돌려보고 고칠 수 있게 하는 것이 목표입니다.

문서가 셋입니다. `입문.md` = 크롤링 개념부터 시작하는 비개발자용,
`README.md` = 쓸 사람용 요약, **여기 = 코드를 만질 사람용**입니다.
크롤링 자체가 처음이라면 `입문.md` §3(크롤링이 어려운 이유)만이라도 먼저 읽고 오세요.
이 문서 §9의 함정들이 왜 생겼는지가 거기 설명돼 있습니다.

---

## 0. 한 문장 요약

> 공모전 사이트를 긁어와서(수집) → 우리 조건 3개로 거르고(필터) → 빈칸을 로컬 LLM으로 채운 뒤(보강)
> → 팀 공지용 엑셀 한 장에 계속 누적하는(출력) 파이썬 스크립트.

사람이 하는 일은 마지막에 엑셀을 열어 `공지 상태`를 검수하는 것뿐입니다.

---

## 1. 5분 만에 돌려보기

이 서버(`eez192`)에서는 아래 명령이 **그대로 동작하는 것을 확인**했습니다.

```bash
cd "/home/schoaq/competition-autoscraper 2"   # 폴더명에 공백이 있으니 따옴표 필수
./run.sh
```

`run.sh`가 알아서 해주는 것: Ollama 서버 기동 → 모델(`qwen2.5:3b`) 확인/다운로드 →
의존성이 깔린 `/usr/bin/python3`로 `main.py` 실행.

가볍게 맛만 보려면 (LLM 없이, 1~2분):

```bash
PAGES=1 MAX_DETAILS=5 LLM_MAX_ITEMS=0 /usr/bin/python3 main.py
```

**주의: `main.py`는 항상 `data/후보_컴피티션_목록.xlsx`에 실제로 행을 추가합니다.**
실험 전에는 백업을 뜨세요 (`data/후보_컴피티션_목록.bak_*.xlsx`가 그 흔적입니다).

```bash
cp data/후보_컴피티션_목록.xlsx "data/후보_컴피티션_목록.bak_$(date +%Y%m%d_%H%M).xlsx"
```

### 확인된 실행 환경

| 항목 | 값 | 비고 |
|---|---|---|
| 파이썬 | `/usr/bin/python3` = 3.10.12 | `requests 2.32.4`, `bs4 4.13.4`, `openpyxl 3.1.5` 설치됨 |
| Ollama | `/home/schoaq/.local/bin/ollama` (v0.32.3) | 서버 기동 중, `qwen2.5:3b` (1.9GB) 보유 |
| conda | 쓰지 않음 | `run.sh`가 시스템 파이썬을 직접 지정 |

`run.sh`의 `OLLAMA`/`PY` 경로는 **이 머신 기준 하드코딩**입니다. 다른 PC에서 쓰려면 이 두 줄만 고치면 됩니다.

---

## 2. 전체 그림 (파이프라인)

```
                 SOURCES 환경변수로 on/off
   ┌──────────────┬──────────────┬──────────────┐
   │ wevity       │ wevity_intl  │ devpost      │   ← scraper/wevity.py, devpost.py
   │ (국내 HTML)  │ (해외 HTML)  │ (JSON API)   │
   └──────┬───────┴──────┬───────┴──────┬───────┘
          └──────────────┴──────────────┘
                         ▼
            공통 스키마 dict 리스트 (§4 참고)
                         ▼
   ┌─────────────────────────────────────────────┐
   │ 2) apply_filters()   scraper/filters.py     │
   │    재학생? / 팀 가능? / 마감 D-14 이상?      │
   │    → '탈락' 하나라도 있으면 버림             │
   │    → 애매하면 '확인필요'로 살려둠            │
   └─────────────────────┬───────────────────────┘
                         ▼
   ┌─────────────────────────────────────────────┐
   │ 3) enrich()          scraper/classify.py    │
   │    키워드로 학부 분류 (항상)                 │
   │    + Ollama가 본문 읽고 팀인원/일정/참가비/   │
   │      자격 JSON 추출 (서버 있을 때만, 상위     │
   │      LLM_MAX_ITEMS 건까지)                   │
   └─────────────────────┬───────────────────────┘
                         ▼
   ┌─────────────────────────────────────────────┐
   │ 4) write_candidates() scraper/output.py     │
   │    확정 목록과 동일한 15컬럼 엑셀에 누적      │
   │    링크·대회명으로 중복 제거 → 신규만 append │
   └─────────────────────┬───────────────────────┘
                         ▼
            data/후보_컴피티션_목록.xlsx  ← 사람이 검수
                         │
                         │ '공지 상태'에 "확정" 기입
                         ▼
   ┌─────────────────────────────────────────────┐
   │ evolve.py (별도 실행, 월 1회)                 │
   │  검수 결과 수확 → 평가셋 → 규칙 진화          │
   │  → data/rules.json → 다음 main.py가 자동 적용 │
   └─────────────────────────────────────────────┘
```

핵심은 **`main.py`(주간 수집)와 `evolve.py`(월간 규칙 개선)가 `data/rules.json`을 통해서만
연결된 별개의 프로그램**이라는 점입니다. 둘은 서로를 직접 호출하지 않습니다.

---

## 3. 파일 지도

| 경로 | 역할 | 처음 볼 때 읽는 순서 |
|---|---|---|
| `main.py` | 파이프라인 전체 흐름 + 소스 레지스트리. 여기부터 읽으세요 | ① |
| `scraper/wevity.py` | 위비티 목록/상세 HTML 크롤링 (HTML형 본보기) | ② |
| `scraper/devpost.py` | Devpost 공개 JSON API 수집 (API형 본보기) | ② |
| `scraper/{thinkcontest,contestkorea,linkareer,campuspick,dacon}.py` | 국내 소스 5종 | ② |
| `scraper/{unstop,mlh,zindi,drivendata,aicrowd}.py` | 해외 소스 5종 | ② |
| `scraper/hkust.py` | HKUST 캘린더·창업센터 — 교내 대회 + HKUST에 게시된 홍콩 외부 대회 (소스 키 `hkust_cal`, `hkust_ec`). 설계는 `docs/superpowers/specs/2026-09-13-hkust-hongkong-sources-design.md` | ② |
| `scraper/youthgov.py` | 홍콩 정부 청년포털 대회 (Statamic JSON API, 제목 검색어별 조회) | ② |
| `scraper/{kstartup,bizinfo,usagov_challenges}.py` | 지원사업·연방과제. 기본 꺼짐 | — |
| `scraper/filters.py` | 조건 3종 판정 (정규식 규칙) | ③ |
| `scraper/classify.py` | 학부 분류 + Ollama 구조화 추출 | ③ |
| `scraper/output.py` | 엑셀 생성/누적, 컬럼 정의 | ④ |
| `evolve.py` | 규칙 진화 엔진 (AlphaEvolve 축소판) | ⑤ (선택) |
| `run.sh` | 로컬 실행 런처 (Ollama 자동 기동) | — |
| `data/후보_컴피티션_목록.xlsx` | **최종 산출물.** 현재 41행 | — |
| `data/rules.json` | 진화된 규칙. 있으면 기본 규칙을 덮어씀. **현재는 없음**(§9-1 참고) | — |
| `data/평가셋.jsonl` | 진화용 정답 12건 (시드) | — |
| `data/evolve_log.json` | 세대별 점수 기록 | — |
| `.github/workflows/*.yml` | 주간 수집 / 월간 진화 자동화 (미연결, §7-2) | — |
| `cron.log` | 이 서버 cron 자동 실행 로그 (§7-1) | — |
| `tests/test_rules.py` | 판정 규칙 회귀 테스트 (네트워크 불필요). 규칙을 만지면 여기부터 돌리세요 | — |
| `tests/test_wevity_live.py` | 위비티 본문 추출 통합 테스트 (네트워크 필요, 약 1분) | — |
| `tests/test_hkust.py` | HKUST 파서 테스트 (저장된 실제 HTML 픽스처, 네트워크 불필요) | — |
| `tests/test_hkust_live.py` | HKUST 두 사이트 스모크 테스트 (네트워크 필요, 약 1분) | — |

`scraper/__init__.py`는 빈 파일입니다(패키지 표시용).

---

## 4. 데이터 스키마 — 이 프로젝트의 계약서

모듈 간에 오가는 것은 **평범한 파이썬 dict 하나**입니다. 새 소스를 추가하든 필터를 고치든
이 키 이름만 지키면 됩니다.

### 4-1. 수집기가 만들어야 하는 키 (필수/권장)

| 키 | 타입 | 설명 | 없으면? |
|---|---|---|---|
| `ix` | str | 소스 내 고유 ID | 로그용, 없어도 동작 |
| `url` | str | 공고 상세 URL | 링크 컬럼 대체값 |
| `homepage` | str | 공식 홈페이지 (있으면 우선) | `url` 사용 |
| `title` | str | 대회명 | 중복 제거 키라 **사실상 필수** |
| `field` | str | 분야/주제 | 학부 분류 입력 |
| `target` | str | 응모대상 원문 | 재학생 판정 입력 |
| `host` | str | 주최/주관 | 빈칸 |
| `period` | str | 접수기간 원문 | 현재 엑셀엔 안 쓰임 |
| `prize` | str | 상금 | 빈칸 |
| `deadline` | `YYYY-MM-DD` str 또는 None | 마감일 | 마감 판정이 '확인필요' |
| `body` | str | 본문 텍스트 (위비티는 8000자로 자름) | 팀/재학생 판정 불가 |
| `lang` | `"ko"` / `"en"` | 언어 | 판정 분기용 |
| `location` | str | 장소 (Devpost만) | 비고에서 생략 |
| `source` | str | 소스 표시명 | `"위비티"`로 표기됨 |

### 4-2. 필터가 붙이는 키

`days_left`, `판정_마감`, `판정_재학생`, `판정_팀` (`통과`/`탈락`/`확인필요`),
`검토상태` (`자동통과` / `확인필요`).

### 4-3. 보강 단계가 붙이는 키

`학부`, `team_size`, `schedule`, `fee`, `eligibility_llm`.
LLM이 부적합으로 보면 `검토상태 = "LLM판정_부적합"`이 되고, **지우지 않고**
`공지 상태 = 신규후보(LLM의심)`으로 표시만 합니다(§9-3).

### 4-4. 엑셀 15컬럼 ← dict 매핑 (`output.py:_row_from_item`)

| # | 엑셀 컬럼 | 값의 출처 |
|---|---|---|
| 1 | 공지월 | 실행일 `YYYY-MM` |
| 2 | 학부 | `학부` (LLM 결과 우선, 없으면 키워드 분류, 그것도 없으면 `미분류`) |
| 3 | 대회명 | `title` |
| 4 | 주최 / 주관 | `host` |
| 5 | 분야 / 주제 | `field` |
| 6 | 신청 마감일 | `deadline` 또는 `확인필요` |
| 7 | 대회 일정 | `schedule` — **LLM이 있어야 채워짐** |
| 8 | 팀 인원 (개인 참가 여부) | `team_size` — **LLM이 있어야 채워짐** |
| 9 | 참가 자격 | `eligibility_llm` → `target` → `확인필요` |
| 10 | 공식 링크 | `homepage` → `url` |
| 11 | 상금 / 혜택 | `prize` |
| 12 | 참가비 | `fee` — **LLM이 있어야 채워짐** |
| 13 | 공지 상태 | `검토상태`를 `output.py`의 `STATUS_LABEL`로 옮긴 값. `신규후보`(셋 다 자동통과) / `신규후보(확인필요)`(근거 부족) / `신규후보(LLM의심)`(LLM이 부적합으로 봄) |
| 14 | 담당자 | 항상 빈칸 (사람이 채움) |
| 15 | 비고 | 자동 생성: `"{소스} 수집 / 판정: 재학생 …, 팀 …, D-…"` |

> 7·8·12번 컬럼이 전부 `확인필요`로 나온다면 **LLM이 안 돌았다는 신호**입니다(§9 참고).
> `period`(접수기간 원문)는 수집만 하고 엑셀에는 쓰지 않습니다. 디버깅용으로 남겨둔 값입니다.

---

## 5. 모듈별 상세

### 5-1. `scraper/wevity.py` — 국내 크롤러

* URL 규칙 (2026-07 기준 실측):
  * 목록 `https://www.wevity.com/?c=find&s=1&gub=2&cidx=5&mode=ing&gbn=list&gp={페이지}`
    (`gub=2&cidx=5` = 응시대상 '대학생', `mode=ing` = 접수중)
  * 상세 `…&gbn=view&ix={id}`
* 목록은 `href`의 `gbn=view` + `ix=` 패턴으로 뽑습니다(클래스 무관).
* 상세는 **공고 영역을 클래스로 먼저 좁힌 뒤** 라벨 텍스트 정규식으로 뽑습니다.
  * `div.cd-area` → 라벨 필드(`분야`, `응모대상`, `접수기간`, `총 상금`, `홈페이지`…)
  * `div.comm-desc` → 상세내용(공모요강) 본문 = `body`
  * 둘 다 못 찾으면 `log.warning` 후 폴백합니다(정보는 페이지 전체에서, 본문은 라벨 텍스트로).
    **수집 건수가 멀쩡한데 판정이 이상하면 로그에서 이 경고부터 찾으세요.** 위비티가
    리뉴얼해 클래스가 바뀌었다는 신호입니다.
* 예전에는 `soup.get_text()`로 **페이지 전체**를 `body`에 넣었는데, 좌측 카테고리 메뉴에
  `공모전전략`·`응시대상자 … 대학생 … 청소년`이 들어 있어서 판정 정규식이 전부 거기에
  걸렸습니다. 실측 5건 전부에서 `대학생`/`누구나`/`제한없음`/`전략`이 매치됐고, 그 결과
  **자격 언급이 아예 없는 공고도 재학생 '통과'로 나왔습니다**(`확인필요`가 뜨지 않음).
  §9-1의 `rules.json` 오염에서 `전략`이 규칙이 된 것도 이 메뉴 문구 때문입니다.
* 요청마다 1초 `sleep`, 실패 시 1회 재시도.
* `scrape_overseas()`는 같은 함수에 `gub=1, cidx=28`(해외 카테고리)만 바꿔 넣은 얇은 래퍼입니다.
* 마감일은 라벨 필드 `접수기간`(`2026-06-01 ~ 2026-07-30`)에서 끝 날짜를 뽑고, 없을 때만
  본문 정규식으로 폴백합니다. 본문에는 `접수기간` 표기가 여러 번 나올 수 있어서
  라벨 값이 더 정확합니다.
* `_extract_labeled_fields()`의 라벨↔값 구분자는 `\s`가 아니라 `[ \t]`입니다. `\s`를 쓰면
  **값이 빈 라벨**(`후원/협찬`이 대표적)이 줄바꿈을 삼켜 다음 줄 전체를 자기 값으로
  가져갑니다. `period`가 자주 비던 원인이 이것이었습니다.

동작 확인 방법 (요청 1페이지만):

```bash
/usr/bin/python3 -c "
from scraper import wevity
ids = wevity.list_contest_ids(pages=1); print(len(ids), ids[:5])
print(wevity.fetch_contest_detail(ids[0])['title'])"
```
→ 2026-07-25 실행 시 23건 수집 확인.

### 5-2. `scraper/devpost.py` — 해외 해커톤

* `https://devpost.com/api/hackathons?page=N&open_state=open` 공개 JSON. 인증 불필요.
* **UA만 넣으면 403이 납니다.** `X-Requested-With: XMLHttpRequest` + `Referer`까지
  브라우저 XHR과 동일하게 맞춰야 통과합니다. 헤더를 함부로 줄이지 마세요.
* API가 참가 자격 전문을 안 주기 때문에 `body`를 제목·장소·테마·기간으로 **합성**합니다.
  그래서 재학생/팀 판정이 `확인필요`로 남는 경우가 많은 게 정상입니다.
* 확인: 2026-07-25 기준 1페이지 9건 정상 수신.

### 5-3. `scraper/filters.py` — 판정 3종

```
check_deadline : deadline 파싱 → 남은 일수 >= 14 면 통과, 못 읽으면 확인필요
check_student  : NEG 패턴 하나라도 걸리면 탈락 → POS 있으면 통과 → 아니면 확인필요
check_team     : NEG 패턴 하나라도 걸리면 탈락 → POS 있으면 통과 → 아니면 확인필요
```

**두 판정 함수의 우선순위는 같아야 합니다.** 예전 `check_team`은 `NEG면서 POS가 없을 때`만
탈락시켰는데, `팀 참가 불가`·`팀 접수는 받지 않습니다` 같은 **부정문 안의 '팀 참가'가
POS로 잡혀** 배제 문구를 무력화했습니다. 즉 개인 참가만 되는 대회가 팀 가능으로 나왔습니다.
지금은 `check_student`와 동일하게 NEG가 이깁니다.

설계 철학은 **"놓치느니 남긴다"** 입니다. 애매하면 버리지 않고 `확인필요`로 표시해
사람이 보게 합니다. `apply_filters()`는 셋 중 하나라도 `탈락`이면 그 항목을 제거합니다.
이 철학이 실제로 작동하려면 판정 입력이 **공고 본문뿐**이어야 합니다(§5-1 참고).

`set_rules()` / `get_rules()`는 모듈 전역 변수를 갈아끼우는 함수로, `rules.json` 적용과
`evolve.py`가 씁니다. **전역 상태를 바꾸므로 한 프로세스 안에서 규칙을 바꾸면
그 뒤 모든 판정에 영향을 줍니다** (진화 루프는 이걸 전제로 동작).

`set_rules()`는 `validate_rules()`를 먼저 돌립니다 — 키 이름, 리스트 여부, 패턴 개수
(≤`MAX_PATTERNS_PER_LIST`), 길이(≤`MAX_PATTERN_LEN`), `re.compile()` 성공까지 확인하고
하나라도 어긋나면 `ValueError`를 냅니다. **검증이 먼저이므로 실패해도 기존 규칙이 그대로
남습니다**(부분 적용 없음). 예전에는 검증이 `evolve.py` 안에만 있어서, 손으로 고쳤거나
깨진 `rules.json`이 그냥 적용됐다가 수집이 다 끝난 판정 시점에 `re.error`로 터졌습니다.
지금은 `main.py`의 `load_evolved_rules()`가 그 `ValueError`를 잡아 기본 규칙으로 넘어갑니다.

### 5-4. `scraper/classify.py` — 학부 분류 + LLM 추출

* `classify_school()`: `title + field`에 학부별 키워드가 몇 개 등장하는지 세서 최다 학부 선택.
  하나도 없으면 `미분류`. 단순 카운트라 동점이면 사전 순서대로 먼저 온 학부가 이깁니다.
* 매칭은 `_keyword_hit()`가 담당합니다. **한글은 부분일치**(조사·합성어 때문),
  **영문은 앞뒤가 영숫자가 아닐 것**을 요구하고, `IT`/`AI`/`SW` 같은 전부 대문자 약어는
  **대소문자를 구분**합니다. 예전에는 소문자 부분일치라 `IT`가 compet**it**ion·dig**it**al에,
  `AI`가 sust**ai**nability에, `SW`가 **Sw**ag에 걸려서 영문 제목이 사실상 전부 공대로
  분류됐습니다(동점도 사전 순서상 공대가 이깁니다). 대문자 구분 덕에 `웹/모바일/IT`,
  `AI 해커톤`, `startup경진대회`처럼 한글에 붙은 영문은 그대로 잡힙니다.
* `llm_enrich()`: Ollama `/api/chat`에 `format: "json"`, `temperature 0`, `num_ctx 4096`,
  본문 4000자로 잘라서 던지고 JSON을 받습니다. 타임아웃 180초.
* **`_OLLAMA_DOWN` 전역 플래그**: 연결 실패가 한 번 나면 그 실행 내내 LLM 호출을 건너뜁니다
  (60건 × 180초 타임아웃으로 멈추는 사고 방지). 즉 첫 항목에서 서버가 안 떠 있으면
  그날은 전부 규칙 기반으로만 나옵니다.
* 파일 상단 주석에 `--- Claude API`라고 적힌 구역이 남아 있는데 **실제 구현은 Ollama**입니다.
  README '구조' 절의 "Claude API 정밀 추출"도 옛 설명입니다. API 키는 어디에도 필요 없습니다.

### 5-5. `scraper/output.py` — 엑셀

* `COLUMNS` 15개가 확정 공지 목록과 1:1로 맞춰져 있습니다. **순서를 바꾸면 안 됩니다**
  (`evolve.py`의 수확 코드가 `row[2]`, `row[12]` 같은 인덱스로 접근합니다).
* 중복 제거: 기존 시트에서 공식 링크(10번째)와 **정규화한 대회명**(3번째)을 한 set에 섞어
  담고, 새 항목의 링크나 정규화 제목이 거기 있으면 건너뜁니다.
  * `_dedup_key()`가 공백·괄호·문장부호·이모지를 지우고 소문자로 맞춥니다. 같은 대회가
    사이트마다 `대학가요제`/`대학 가요제`, `『…』`/`…`처럼 표기만 다르게 올라오기 때문입니다.
  * **단어가 더 붙은 경우는 일부러 안 잡습니다**(`…학술대회` vs `…학술대회 공모전`).
    유사도 매칭까지 가면 `Global Hack Week: Data`와 `: Agents`를 합쳐버립니다.
    실측하면 유사 18쌍 중 5쌍이 잡히고 13쌍은 남습니다(§9-5-1).
  * 빈 문자열은 키로 넣지 않습니다. 넣으면 링크 없는 항목들이 서로를 막습니다.
* 파일이 없으면 헤더 서식(남색 배경/흰 굵은 글씨/틀 고정)을 만들고, 있으면 뒤에 append만 합니다.
  → **서식은 최초 생성 때만 적용**됩니다.

### 5-6. `evolve.py` — 규칙 진화 (선택 기능)

`python evolve.py` 한 번에 두 가지를 합니다.

1. **수확(`harvest_from_xlsx`)**: 엑셀 `공지 상태` 칸에 사람이 "확정"이라고 적은 행을
   `data/평가셋.jsonl`에 `student_ok:true, team_ok:true` 정답 사례로 추가합니다.
   → **사람이 엑셀에 "확정"을 직접 써 넣어야만 학습 데이터가 쌓입니다.**
2. **진화(`evolve`)**: 현재 규칙을 시조로 개체군 생성 → 평가셋 정확도로 채점 →
   상위 2개 생존 → 변이(50% 확률로 LLM 제안, 실패하면 오답 본문에서 토큰을 뽑아 추가하는
   휴리스틱) → 세대 반복 → 최고 점수를 `data/rules.json`에 저장.

안전장치: 진화 대상은 **코드가 아니라 정규식·키워드 JSON뿐**이며, 후보는 개체군 진입 전
구조 검사 + `re.compile()` 검증 + 길이/개수 상한(패턴 80자, 리스트당 30개)을 통과해야 합니다.
임의 코드를 생성하거나 실행하지 않습니다.

---

## 6. 환경변수 전체 목록

| 변수 | 기본값 | 쓰는 곳 | 설명 |
|---|---|---|---|
| `SOURCES` | 공모전 소스 13종 (§3 표) | main | 콤마 구분. 모르는 이름은 경고 후 건너뜀. `kstartup`/`bizinfo`/`usagov`는 기본에서 빠져 있으니 쓰려면 직접 적어야 함 |
| `PAGES` | `3` | main | 목록 페이지 수. 일부 해외 소스는 `PAGES-1`, 단일 페이지 소스(mlh/zindi/drivendata/aicrowd)는 무시 |
| `MAX_DETAILS` | `60` | main | **소스당** 상세 수집 상한. 소스가 13개이므로 이 값이 전체 실행 시간을 거의 결정한다 |
| `LLM_MAX_ITEMS` | `40` | main | LLM으로 보강할 건수 상한. 초과분은 규칙 기반만 |
| `OLLAMA_HOST` | `http://localhost:11434` | classify, evolve | **`http://` 반드시 포함** (그대로 URL로 씀) |
| `OLLAMA_MODEL` | `qwen2.5:3b` | classify, evolve | 한국어 중시하면 `exaone3.5:2.4b` |
| `GENERATIONS` | `8` | evolve | 세대 수 |
| `POPULATION` | `6` | evolve | 개체 수 |

한 소스가 예외로 죽어도 `main.py`가 잡아서 로그만 남기고 나머지 소스는 계속 갑니다.

---

## 7. 자동 실행

자동화 경로는 두 가지입니다. **현재 살아 있는 것은 7-1(이 서버의 cron)뿐**이고,
7-2(GitHub Actions)는 코드만 있고 아직 연결되지 않았습니다.

### 7-1. 이 서버의 cron — ✅ 2026-07-25 설정 완료, 동작 중

```
0 8 * * 1 /usr/bin/flock -n /tmp/competition-scraper.lock "/home/schoaq/competition-autoscraper 2/run.sh" >> "/home/schoaq/competition-autoscraper 2/cron.log" 2>&1
```

* **매주 월요일 08:00에 실행됩니다.** ⚠️ 이 서버의 시간대는 KST가 아니라
  **HKT(Asia/Hong_Kong, UTC+8)** 입니다. 08:00 HKT = **09:00 KST**.
  시간을 바꾸려면 `crontab -e`에서 `0 8`의 숫자만 고치면 됩니다 (한국시간 기준 -1시간).
* `flock -n`: 지난주 실행이 아직 안 끝났으면 이번 회차를 **건너뜁니다**. 중복 실행 방지용이고,
  건너뛸 때 종료코드 1을 남깁니다.
* 로그는 프로젝트 루트 `cron.log`에 **누적**됩니다 (1회 실행당 약 20줄이라 방치해도 무방).
  `data/` 밖에 두었으므로 나중에 git에 올려도 커밋되지 않습니다.
* `run.sh`가 Ollama 서버 기동·모델 확인까지 하므로 **사전 준비 없이 그대로 돌아갑니다.**
  서버가 재부팅돼 Ollama가 죽어 있어도 첫 실행 때 알아서 다시 띄웁니다.

확인·정지 방법:

```bash
crontab -l                                   # 등록 상태 확인
tail -30 "/home/schoaq/competition-autoscraper 2/cron.log"   # 지난 실행 결과
crontab -e                                   # 해당 줄 앞에 #을 붙이면 일시 정지
```

> **공유 서버 주의:** Ollama는 VRAM이 가장 큰 GPU를 자동으로 고릅니다. 실측 결과
> **GPU 5(RTX 4090 D)** 에 약 3.6GB를 잡았고, 유휴 5분 뒤 자동 해제됩니다.
> 주 1회 1~2분이라 부담은 작지만, GPU 5를 다른 작업에 쓰고 있다면
> `run.sh`의 Ollama 기동 줄에 `CUDA_VISIBLE_DEVICES=<번호>`를 붙여 다른 GPU로 고정하세요
> (이 서버는 GPU 5만 4090이고 나머지는 3090이라 인덱스 주의 — `CUDA_DEVICE_ORDER=PCI_BUS_ID` 병행 권장).

### 7-2. GitHub Actions — 아직 미연결

| 워크플로 | 주기 | 하는 일 |
|---|---|---|
| `scrape.yml` | 매주 월 09:00 KST | 의존성 설치 → Ollama 설치·모델 pull → `main.py` → `data/` 커밋·푸시 |
| `evolve.yml` | 매월 1일 10:00 KST | 같은 준비 → `evolve.py` → `rules.json`/`평가셋` 커밋·푸시 |

* **현재 이 폴더는 git 저장소가 아닙니다** (`git status` → not a repository).
  Actions를 쓰려면 `git init` → 원격 추가 → push가 먼저입니다. `.DS_Store`는 macOS 잔재이니
  `.gitignore`에 `cron.log`, `data/*.bak*`과 함께 넣으세요. 폴더명 끝의 ` 2`도 Finder 복제본 흔적입니다.
* 무료 러너에는 GPU가 없어 3B 모델도 **1건당 30초~1분**입니다. `LLM_MAX_ITEMS=40`이
  그래서 있는 값이고, 이걸 올리면 120분 타임아웃에 걸릴 수 있습니다.
  (참고: GPU가 있는 이 서버에서는 40건 보강이 통째로 **약 60초**에 끝납니다. 로컬이 훨씬 빠릅니다.)
* 퍼블릭 레포는 Actions 무제한, 프라이빗은 월 2,000분 한도입니다.
* 워크플로가 `git push`를 하므로 `permissions: contents: write`가 필요합니다(이미 설정됨).
* **두 경로를 동시에 켜지 마세요.** 같은 엑셀에 각자 행을 추가해 충돌합니다.
  Actions로 옮길 때 7-1의 cron 줄을 주석 처리하세요.
* `evolve.yml`은 §9-1 문제가 해결되기 전까지 켜지 않는 편이 안전합니다.

---

## 8. 새 수집 소스 추가하기

파일 하나 + 등록 한 줄이면 끝납니다.

```python
# scraper/mysite.py
def scrape(pages: int = 2) -> list[dict]:
    return [{
        "source": "마이사이트", "ix": "mysite-1",
        "url": "...", "homepage": "...", "title": "...",
        "field": "...", "target": "...", "host": "...",
        "prize": "...", "deadline": "2026-09-30",   # YYYY-MM-DD 또는 None
        "body": "판정에 쓸 본문 텍스트",
        "lang": "ko",
    }]
```

```python
# main.py
from scraper import wevity, devpost, mysite            # ①
SOURCE_REGISTRY = {
    ...,
    "mysite": lambda p, m: mysite.scrape(pages=p),     # ②
}
```

`SOURCES=mysite python main.py`로 그 소스만 따로 테스트할 수 있습니다.
**요청 사이 1초 이상 딜레이를 꼭 넣으세요** (기존 소스와 동일한 예의 수준 유지).

**`body`에는 공고 본문만 담으세요.** 페이지 전체 텍스트를 넣으면 그 사이트의 메뉴·사이드바가
판정 정규식에 걸려 모든 공고가 '통과'로 나옵니다(§5-1이 그 사고 기록입니다).
본문 요소를 못 찾으면 전체 텍스트로 폴백하지 말고, `log.warning` 후 제목·분야·기간 정도의
짧은 요약을 넣는 편이 낫습니다 — 정보가 없으면 `확인필요`로 남아 사람이 보게 됩니다.

**목록이 안 보이면 내부 JSON부터 찾으세요.** "JS 렌더링이라 불가"로 적혀 있던 씽굿·링커리어·
캠퍼스픽·MLH·Unstop은 전부 내부 JSON/GraphQL을 쓰고 있었고, 헤드리스 브라우저 없이
추가했습니다. HTML에 목록이 없으면 `/api/`, `/graphql`, `__NEXT_DATA__`,
`window.__INITIAL_STATE__`, `ld+json`을 뒤져보세요.

Kaggle은 공식 API가 있지만 키가 필요하고, HackerEarth는 브라우저 UA로도 403입니다.

---

## 9. 알려진 함정 / 주의사항

작업 전에 이 절만은 읽어두면 시간을 아낍니다.

1. **`evolve.py`를 돌리면 `rules.json`이 다시 오염될 수 있습니다.** ⚠️ 가장 중요한 항목
   2026-07-25에 한 번 실제로 발생했습니다. 진화 결과 `team_pos`에 `전략`, `경우`, `are`가,
   `student_pos`에 `individuals`가 붙어 있었습니다. 실측하면:

   | 패턴 | 실제 공고 13건 중 매치 | 결과 |
   |---|---|---|
   | `전략` | 8건 | 위비티 사이드바 문구에 흔함 |
   | `경우` | 5건 | 일반 안내문에 흔함 |

   이 때문에 **팀 판정이 13건 중 6건에서 `확인필요` → `통과`로 잘못 뒤집혔습니다.**
   개인 참가만 되는 대회도 팀 가능으로 표시된다는 뜻입니다.
   현재는 파일을 제거해 기본 규칙으로 되돌린 상태입니다
   (백업: `data/rules.json.polluted_bak_20260725_1924`, 재발 시 그냥 지우면 기본 규칙 사용).

   **원인은 파일이 아니라 평가 방식입니다.** 평가셋 12건 중 부정 사례가 student/team 각 3건뿐이라
   "웬만하면 통과"가 만점 전략이 되고(실측: 무조건 `통과` 0.750 vs 무조건 `확인필요` 0.400 —
   `확인필요`에 부분점수 0.4를 주는 `_judge_to_score`가 느슨한 규칙을 보상합니다),
   `mutate_heuristic()`이 오답 본문에서 아무 토큰이나 뽑아
   패턴으로 추가하기 때문에 `경우` 같은 흔한 말이 규칙이 됩니다
   (오염 규칙 점수 1.000 vs 기본 규칙 0.925 — 평가셋에서만 더 높음).
   게다가 수확(`harvest_from_xlsx`)은 사람이 "확정"한 건, 즉 **긍정 사례만** 쌓으므로
   불균형은 시간이 갈수록 심해집니다.

   결정적으로 **평가셋 본문 평균이 39자인데 실제 공고 본문은 1,000자대**입니다.
   노이즈에 반응하는 패턴은 평가셋에서 비용이 0이라 적합도가 그 손해를 볼 수가 없습니다.
   부정 사례를 추가해도 이 분포 차이는 그대로입니다.

   > **지금까지 손본 것과 안 손본 것:**
   > `set_rules()`에 형식 검증이 붙어서 **깨진 규칙 파일이 실행을 죽이는 일은 없어졌고**,
   > §5-1의 본문 스코프 축소로 `전략` 같은 메뉴 문구가 애초에 본문에 안 들어옵니다.
   > 하지만 **적합도 함수가 잘못된 것을 보상하는 문제는 그대로입니다** — 오염된
   > `rules.json.polluted_bak_*`를 지금 검증기에 넣어도 "형식상 유효"로 통과합니다.
   > 홀드아웃 분리와 "기본 규칙보다 못하면 저장하지 않음" 게이트가 붙기 전까지
   > `evolve.yml`은 켜지 마세요.

   → **진화를 다시 돌리기 전에** 부정 사례(`student_ok:false` / `team_ok:false`)를
   평가셋에 손으로 20~30건 추가하세요. 그 전까지는 `evolve.yml` 워크플로를 꺼두는 편이 안전합니다.
   진화 후에는 반드시 `rules.json`의 diff를 눈으로 확인하세요:
   ```bash
   /usr/bin/python3 -c "
   import json; from scraper import filters, classify
   s = json.load(open('data/rules.json', encoding='utf-8'))['config']
   for k, v in filters.get_rules().items():
       add = [p for p in s['filters'][k] if p not in v]
       if add: print(k, '추가됨:', add)"
   ```

2. **LLM이 없으면 엑셀 절반이 `확인필요`입니다.** 팀 인원·일정·참가비는 규칙 기반으로는
   못 채웁니다. 정상 동작이지 버그가 아닙니다.

3. **작은 모델은 프롬프트를 베낍니다.** ⚠️ 2026-07-26에 실제로 크게 데인 항목

   `EXTRACT_PROMPT`의 JSON 스키마에 예시를 값 자리에 넣어뒀었습니다.

   ```
   "team_size": "팀 인원 (개인 참가 여부 포함, 예: '2~4인 / 개인 참가 불가')",
   "fee":       "참가비 (예: '무료' 또는 금액)",
   "schedule":  "대회 일정 요약 (예선/본선 날짜 등)",     ← 여기만 구체 예시 없음
   ```

   qwen2.5:3b는 값 자리의 문장을 **채울 틀이 아니라 베낄 본보기로** 취급합니다. 결과:

   | 컬럼 | 프롬프트에 예시 | 실측 결과 |
   |---|---|---|
   | `team_size` | 있음 | 채워진 157건 중 107건이 예시 문자열 두 개, 3건은 설명문 자체 |
   | `fee` | 있음 | 채워진 229건 중 200건이 `무료`. 본문에 참가비를 언급한 공고는 소수 |
   | `schedule` | **없음** | 최빈값 비중 4%. 유일하게 제대로 추출됨 |

   지금은 **설명을 JSON 밖으로 빼고 출력 틀은 빈 값만** 남겼습니다. 같은 공고 6건 재검증에서
   `fee`가 6/6 `무료` → 6/6 `확인필요`로 바뀌었고, `team_size`는 본문에 팀 언급이 있는
   건만 값이 나옵니다. **스키마 값 자리에 예시나 설명을 넣지 마세요.**

   같은 이유로 `student_ok`/`team_ok`도 근거 없이 `false`가 찍혔습니다. 예전에는 그게
   곧 삭제였는데(2026-07-26 실행에서 299건 중 35건이 조용히 사라짐), 지금은
   **삭제하지 않고 `신규후보(LLM의심)`으로 표시만** 합니다. "놓치느니 남긴다"는 원칙이
   LLM 단계에도 적용된 셈입니다. 표시 건수는 실행 로그에 나옵니다.

4. ~~링크가 빈 문자열인 항목은 한 실행에 하나만 들어갑니다.~~ **수정됨** — `output.py`가
   빈 문자열을 중복 키로 넣지 않습니다(`tests/test_output.py`가 지킵니다). 그래도 새 소스를
   만들 때 `url`/`homepage`는 채우세요. 링크가 없으면 대회명만으로 중복을 판정하게 됩니다.

5. **엑셀은 계속 커지기만 합니다.** 지난 대회를 지우는 로직이 없습니다. 정리는 수동입니다.

5-1. **같은 대회가 여러 소스에서 각각 한 줄씩 들어올 수 있습니다.** 국내 소스 5곳
   (위비티/씽굿/콘테스트코리아/링커리어/캠퍼스픽)은 서로 겹치는 공고가 있는데,
   `output.py`의 중복 제거는 공식 링크·대회명이 **정확히 일치할 때만** 걸립니다.
   기본값 전체 실행 실측(2026-07-26): 297건 중 34건은 정확 일치로 걸러졌지만,
   **남은 263행 중 18쌍(약 7%)이 사실상 같은 대회**입니다. 차이가 한 글자 수준이라 그렇습니다.

   | 소스 A | 소스 B | 차이 |
   |---|---|---|
   | `2026 MBC 대학가요제` | `2026 MBC 대학 가요제` | 띄어쓰기 |
   | `『제1회 …공모대회』` | `제1회 …공모대회` | 괄호 |
   | `…청년외교광장 학술대회` | `…청년외교광장 학술대회 공모전` | 꼬리말 |
   | `2026 시결 신인상 공모` | `2026년 시결 신인상 공모` | `년` |
   | `제9회 전국 청년 아이디어톤 대회` | `제9회 청년 아이디어톤 대회` | `전국` |

   **지금은 공백·괄호·문장부호를 지운 정규화 키를 씁니다**(§5-5). 위 1·2번과 이모지·따옴표
   차이가 잡혀서 18쌍 중 5쌍이 합쳐지고, **3~5번 같은 13쌍은 그대로 남습니다.**
   유사도 매칭까지 가면 서로 다른 대회(`Global Hack Week: Data` vs `: Agents`)를
   합쳐버리므로 일부러 거기까지 가지 않았습니다. 남는 건 비고란의 소스 표기로 사람이 판별하세요.

6. **폴더명에 공백이 있습니다.** `cd /home/schoaq/competition-autoscraper 2`는 실패합니다.
   항상 따옴표를 쓰세요.

7. **수집 예의.** 요청 간 1초 딜레이가 들어 있습니다. 속도를 올리고 싶어도 이 값은 건드리지 말고
   위비티 이용약관을 확인하세요. 수집 데이터는 내부 공지용으로만 씁니다.

---

## 10. 문제 생겼을 때 체크리스트

| 증상 | 먼저 볼 곳 |
|---|---|
| 수집 0건 | 위비티 URL 규칙이 바뀌었는지. `list_contest_ids(pages=1)`을 직접 호출해 확인 |
| Devpost 403 | `devpost.py`의 `HEADERS`에서 `X-Requested-With`/`Referer`가 빠지지 않았는지 |
| 팀 인원·참가비가 전부 `확인필요` | `curl -s http://127.0.0.1:11434/api/version` → 서버 죽었으면 `./run.sh`가 살려줌 |
| LLM이 느림/멈춤 | `LLM_MAX_ITEMS`를 줄이기. 타임아웃은 건당 180초 |
| 판정이 이상하게 다 통과 | `data/rules.json` 오염 (§9-1). 지우면 코드 기본 규칙으로 돌아감. 로그에 `본문 영역(.comm-desc) 없음` 경고가 있으면 위비티 리뉴얼 (§5-1) |
| 규칙을 고친 뒤 | `/usr/bin/python3 tests/test_rules.py` — 12건 평가셋 회귀까지 확인함 |
| 엑셀에 아무것도 안 늘어남 | 전부 중복 판정. 로그의 `신규 N건`과 필터 통과 건수를 비교 |
| 마감일이 `확인필요` | `wevity.py`의 접수기간 정규식이 본문 형식과 안 맞는 것. 본문을 찍어보고 패턴 보완 |
| 월요일에 자동 실행이 안 됨 | `crontab -l`로 등록 확인 → `cron.log` 확인 → `pgrep cron`으로 데몬 확인. 시간대가 HKT임에 유의(§7-1) |

로그는 `INFO` 레벨로 표준출력에 나오며, 단계마다 `=== 1) 수집 ===` 식으로 구분됩니다.
단계별 건수만 따라가도 어디서 새는지 대부분 잡힙니다.
