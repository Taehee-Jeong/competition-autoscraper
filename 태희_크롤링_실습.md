# 태희 실습 — 크롤링·스크래핑 직접 해보기

읽기만 하는 문서가 아닙니다. **명령어를 하나씩 직접 쳐보면서** 진행하세요.
개념 설명은 `입문.md`에 있으니 그것부터 읽고 오시면 좋고, 안 읽으셔도 따라올 수 있게 썼습니다.

준비물은 이것뿐입니다.

```bash
cd "/home/schoaq/competition-autoscraper 2"    # 폴더 이름에 공백이 있어서 따옴표 필수
```

각 단계마다 **"이렇게 나오면 성공"** 을 적어뒀습니다. 다르게 나오면 그 자리에서 멈추고
왜 다른지 보세요. 크롤링은 원래 예상과 다르게 나오는 일이 절반입니다.

---

## 1단계 — 웹페이지를 글자로 받아보기

브라우저 없이 파이썬이 직접 서버에 "이 주소 내용 주세요"라고 물어봅니다.

```bash
/usr/bin/python3 -c "
import requests
r = requests.get('https://www.wevity.com/?c=find&s=1&gbn=view&gp=1&ix=107425',
                 headers={'User-Agent': 'Mozilla/5.0'})
print('상태코드:', r.status_code)
print('받은 글자 수:', len(r.text))
print(r.text[:300])
"
```

**이렇게 나오면 성공:** 상태코드 `200`, 글자 수 수만 자, 그리고 `<!DOCTYPE html>`로 시작하는
꺾쇠투성이 글자.

> **상태코드**는 서버의 대답입니다. `200`=여기 있습니다, `404`=그런 거 없습니다,
> `403`=너한테는 안 줍니다, `500`=서버가 고장났습니다.

### 왜 `User-Agent`를 넣나요

`User-Agent`는 "나는 이런 프로그램입니다"라고 밝히는 자기소개입니다. 안 밝히면
파이썬 기본값인 `python-requests/2.x`가 나가고, 그걸 보고 막는 사이트가 있습니다.

```bash
/usr/bin/python3 -c "
import requests
for name, url in [('HackerEarth', 'https://www.hackerearth.com/challenges/'),
                  ('Devpost',     'https://devpost.com/api/hackathons?open_state=open')]:
    a = requests.get(url, timeout=15).status_code
    b = requests.get(url, headers={'User-Agent':'Mozilla/5.0'}, timeout=15).status_code
    print(f'{name:12} UA없이 {a} / UA넣고 {b}')
"
```

**이렇게 나옵니다:**

```
HackerEarth  UA없이 403 / UA넣고 403     ← 아예 봇을 막는 사이트. 우리는 포기했습니다
Devpost      UA없이 200 / UA넣고 200     ← 지금은 UA 없이도 열려 있습니다
```

Devpost는 **예전에 UA 없이 403을 줬고** 그래서 `scraper/devpost.py`에 헤더가 잔뜩 들어 있습니다.
지금은 안 막지만 언제 다시 막을지 모르니 그대로 둡니다. 브라우저인 척 속이는 게 아니라
**브라우저가 원래 보내는 정보를 우리도 보내주는 것**이라고 생각하시면 됩니다.

HackerEarth처럼 헤더를 넣어도 `403`이면 **"막았구나" 하고 물러납니다.** 뚫지 않습니다.

---

## 2단계 — 글자 더미에서 원하는 것만 뽑기 (스크래핑)

받아온 HTML은 사람이 읽기 힘듭니다. `BeautifulSoup`이 그걸 정리해줍니다.

```bash
/usr/bin/python3 -c "
import requests
from bs4 import BeautifulSoup
r = requests.get('https://www.wevity.com/?c=find&s=1&gbn=view&gp=1&ix=107425',
                 headers={'User-Agent':'Mozilla/5.0'})
r.encoding = r.apparent_encoding
soup = BeautifulSoup(r.text, 'html.parser')

print('제목:', soup.find('meta', property='og:title')['content'])
print()
print('--- 정보 영역만 ---')
for li in soup.find('div', class_='cd-area').find_all('li')[:6]:
    print(' ', li.get_text(' ', strip=True))
"
```

**이렇게 나오면 성공:**

```
제목: 2026 한국수력원자력 유튜브 짧은영상 공모전 - WEVITY

--- 정보 영역만 ---
  분야 광고/마케팅, 영상/UCC/사진, ...
  응모대상 제한없음
  주최/주관 한국수력원자력
```

`find('div', class_='cd-area')`가 **"페이지 전체가 아니라 이 상자 안에서만 찾아"** 라는 뜻입니다.
이게 이 실습에서 제일 중요한 한 줄입니다. 4단계에서 이유가 나옵니다.

---

## 3단계 — 목록에서 상세로 타고 들어가기 (크롤링)

지금까지는 주소를 제가 알려드렸습니다. 실제로는 **목록 페이지에서 주소를 긁어모아야** 합니다.

```bash
/usr/bin/python3 -c "
from scraper import wevity
ids = wevity.list_contest_ids(pages=1)
print(f'대회 {len(ids)}건 발견:', ids[:5])
d = wevity.fetch_contest_detail(ids[0])
print()
print('제목  :', d['title'])
print('마감  :', d['deadline'])
print('응모대상:', d['target'])
print('본문  :', d['body'][:120].replace(chr(10), ' '))
"
```

**이렇게 나오면 성공:** 20건 안팎의 숫자 ID가 나오고, 첫 대회의 제목·마감일이 찍힙니다.

이게 크롤링(목록 돌며 주소 모으기)과 스크래핑(각 페이지에서 값 뽑기)이 합쳐진 모습입니다.
`scraper/wevity.py`를 열어보면 방금 한 일이 그대로 적혀 있습니다.

---

## 4단계 — 함정: 메뉴가 딸려옵니다 ⚠

이 프로젝트에서 **실제로 크게 데인 부분**입니다. 직접 확인해보세요.

```bash
/usr/bin/python3 -c "
import requests
from bs4 import BeautifulSoup
r = requests.get('https://www.wevity.com/?c=find&s=1&gbn=view&gp=1&ix=107425',
                 headers={'User-Agent':'Mozilla/5.0'})
r.encoding = r.apparent_encoding
soup = BeautifulSoup(r.text, 'html.parser')

전체 = soup.get_text()
본문만 = soup.find('div', class_='comm-desc').get_text()

for 낱말 in ['대학생', '청소년', '누구나', '전략']:
    print(f'{낱말:5} 전체페이지 {전체.count(낱말)}번 / 공고본문 {본문만.count(낱말)}번')
"
```

**이렇게 나옵니다:**

```
대학생   전체페이지 1번 / 공고본문 0번
청소년   전체페이지 1번 / 공고본문 0번
누구나   전체페이지 1번 / 공고본문 0번
전략     전체페이지 2번 / 공고본문 0번
```

(숫자는 그날 페이지 상태에 따라 조금 달라집니다. **중요한 건 오른쪽이 전부 0**이라는 점입니다.)

이 공고 본문에는 `대학생`이라는 말이 **한 번도 안 나옵니다.** 그런데 페이지 전체를 보면 나옵니다.
어디서 왔을까요? 왼쪽 메뉴의 `응시대상자: 전체 / 제한없음 / 일반인 / 대학생 / 청소년`,
그리고 `공모전전략`이라는 메뉴 항목입니다.

봇은 "글에 `대학생`이 있으면 대학생이 참가할 수 있는 대회"라고 판단합니다.
그래서 예전에는 **참가 자격을 한 글자도 안 밝힌 공고까지 전부 "대학생 참가 가능"** 으로 나왔습니다.
`전략`은 더 나빠서, 규칙을 스스로 고치는 기능이 그걸 "팀 참가 가능 신호"로 학습해버렸습니다.

> **기억할 것:** 페이지 전체를 퍼오지 말고 **공고 본문만** 잘라내세요.
> 새 사이트를 붙일 때 이것 하나만 지키면 절반은 성공입니다.

---

## 5단계 — API가 있으면 API를 쓰세요

HTML을 뜯는 건 최후의 수단입니다. 많은 사이트가 화면을 그리려고 **이미 데이터를 따로 받고 있고**,
그 통로를 우리도 쓸 수 있습니다.

```bash
/usr/bin/python3 -c "
import requests
r = requests.get('https://app.dacon.io/api/v1/competition/list',
                 params={'offset':0, 'limit':3},
                 headers={'User-Agent':'Mozilla/5.0', 'Accept':'application/json'})
print('상태코드:', r.status_code)
for c in r.json()['data'][:3]:
    print(f\"  {c['name'][:40]:<42} 마감 {c['period_end'][:10]}  상금 {c['prize']}만원\")
"
```

**이렇게 나오면 성공:**

```
상태코드: 200
  제3회 풍력발전량 예측 AI 경진대회 - BARAM 2026     마감 2026-08-14  상금 2000만원
```

**차이가 보이시나요.** 꺾쇠를 뜯을 필요 없이 `c['name']`, `c['period_end']`로 바로 값이 나옵니다.
디자인이 바뀌어도 안 깨집니다.

> **오늘 실제로 겪은 일:** 이 자리에 원래 Devpost API 예제를 넣으려 했는데, 하루에
> 수집을 네 번 돌렸더니 **Devpost가 `403`을 주기 시작했습니다.** 어제까진 잘 되던 곳입니다.
> 이게 요청 사이에 1초씩 쉬는 이유입니다 — 너무 자주 두드리면 막힙니다.
> 막히면 며칠 기다리거나 요청 간격을 늘려야 합니다.

### API 찾는 법

1. 브라우저에서 목록 페이지를 엽니다
2. `F12` → **Network** 탭 → `Fetch/XHR` 필터
3. 페이지를 새로고침하거나 다음 페이지를 누릅니다
4. 목록 데이터가 담긴 요청을 찾습니다 (보통 `.json`, `/api/`, `/graphql`)

이 프로젝트의 씽굿·링커리어·데이콘·Unstop·MLH가 전부 이렇게 찾은 것입니다.
예전 문서에는 "이 사이트들은 자바스크립트라서 수집 불가"라고 적혀 있었는데, 실제로는
API를 안 찾아봤을 뿐이었습니다.

---

## 6단계 — 예의와 규칙 (건너뛰지 마세요)

기술적으로 되는 것과 해도 되는 것은 다릅니다.

### robots.txt 확인

```bash
/usr/bin/python3 -c "
import requests
for site in ['https://www.wevity.com', 'https://news.google.com', 'https://search.naver.com']:
    t = requests.get(site + '/robots.txt', headers={'User-Agent':'Mozilla/5.0'}, timeout=10).text
    print(f'--- {site} ---')
    print('\n'.join(t.splitlines()[:6]))
    print()
"
```

**보시면:** 네이버 검색과 구글 뉴스는 `Disallow: /` — **전면 금지**입니다.
실제로 이 프로젝트에서 뉴스 수집을 검토했다가 이것 때문에 접었습니다.
"막혀 있으니 뚫자"가 아니라 **"금지면 안 한다"** 가 원칙입니다.

### 요청 사이에 쉬기

이 프로젝트는 요청마다 1초씩 쉽니다. `scraper/wevity.py`의 `time.sleep(delay)`가 그것입니다.
빨리 하고 싶어도 이 값은 건드리지 마세요. 쉬지 않고 두드리면 상대 서버에 부담이고,
차단당하면 그날부터 아무것도 못 가져옵니다.

---

## 7단계 — 실습 과제: 눈으로 확인하기

### 과제 1. 본문이 정말 깨끗한지 확인

```bash
/usr/bin/python3 tests/test_wevity_live.py
```

**성공하면:** `OK: 본문에 메뉴 텍스트 없음, 필드 전건 채워짐`

이 테스트는 실제로 위비티에서 5건을 받아서 본문에 메뉴 낱말(`공모전전략`, `메일링 신청`,
`응시대상자` …)이 섞였는지 검사합니다. 4단계의 사고가 다시 안 나게 지키는 장치입니다.

### 과제 2. 판정 규칙이 어떻게 작동하는지 보기

```bash
/usr/bin/python3 -c "
from scraper.filters import check_student, check_team
for 본문 in ['대학생 및 휴학생, 팀 단위(2~5인) 접수',
             '초·중·고 재학생만 참가 가능',
             '개인 참가만 가능하며 팀 접수는 받지 않습니다',
             '출품작은 A4 5매 이내로 제출합니다']:
    print(f'{본문[:32]:<34} 재학생={check_student({\"body\":본문})} 팀={check_team({\"body\":본문})}')
"
```

마지막 줄이 **`확인필요`** 로 나오는 게 핵심입니다. 정보가 없을 때 "안 된다"고 단정하지 않고
사람에게 넘깁니다. 이 프로젝트의 원칙이 **"놓치느니 남긴다"** 입니다.

### 과제 3. 새 사이트 하나 붙여보기

가장 큰 과제입니다. 순서는 이렇습니다.

1. 목표 사이트의 `robots.txt` 확인 → 금지면 거기서 끝
2. `F12` → Network → Fetch/XHR 로 **API부터** 찾기
3. 없으면 HTML에서 **공고 본문만 담긴 요소** 찾기 (4단계!)
4. `scraper/devpost.py`(API형)나 `scraper/wevity.py`(HTML형)를 본떠 파일 하나 만들기
5. `main.py`의 `SOURCE_REGISTRY`와 `SOURCE_INFO`에 한 줄씩 등록
6. `SOURCES=내소스 PAGES=1 MAX_DETAILS=5 /usr/bin/python3 main.py` 로 시험

반환해야 하는 모양은 `DEVELOPMENT.md` §4에 표로 있습니다. 막히면 그 문서 §8을 보세요.

---

## 8단계 — 스스로 확인하기

답이 바로 안 떠오르면 해당 단계로 돌아가세요.

1. `403`이 왔습니다. 무엇부터 확인하시겠어요?
2. 공고 본문에 `대학생`이 없는데 봇이 "대학생 가능"이라고 합니다. 어디를 보시겠어요?
3. 목록이 HTML에 안 보입니다. 포기하기 전에 무엇을 해보시겠어요?
4. 어떤 사이트의 `robots.txt`에 `Disallow: /`가 있습니다. 어떻게 하시겠어요?
5. 판정이 `확인필요`로 나왔습니다. 이건 오류인가요?

<details>
<summary>답 보기</summary>

1. `User-Agent` 헤더. 그래도 안 되면 `Referer`, `X-Requested-With`. (Devpost가 그랬습니다)
2. 페이지 전체 텍스트를 쓰고 있는지. 메뉴·사이드바가 섞였을 겁니다. (4단계)
3. `F12` → Network → Fetch/XHR 에서 내부 API 찾기. 씽굿·링커리어가 그렇게 붙었습니다.
4. 안 합니다. 다른 통로를 찾습니다.
5. 오류가 아닙니다. 근거를 못 찾았으니 사람이 봐 달라는 뜻입니다.

</details>

---

## 더 볼 것

| 무엇 | 어디 |
|---|---|
| 크롤링이 뭔지, 왜 이렇게 만들었는지 | `입문.md` |
| 검수 웹 쓰는 법 | `README.md` |
| 코드 구조와 함정 모음 | `DEVELOPMENT.md` (특히 §9) |
| 실제 수집기 예제 | `scraper/wevity.py`(HTML형), `scraper/devpost.py`(API형) |

검수 웹은 `http://143.89.46.192:8000` 입니다. 지금 264건이 올라가 있으니
실습하면서 실제 공고를 눌러보셔도 됩니다.
